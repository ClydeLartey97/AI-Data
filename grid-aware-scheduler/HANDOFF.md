# HANDOFF.md — Read this first

**Purpose of this file:** if you are an AI assistant opening this folder for the first time, this document tells you everything you need to know to continue the work with no other context. Read this fully before touching any code.

**Protocol:** whichever tool is used in a working session, at the end of that session, update the "Current State" and "Next Steps" sections below, AND append a dated entry to "Session Log" (never edit or delete past log entries). Do not let this file go stale. It is the single source of truth for the project, not the conversation history in any one chat.

**"Catch up" protocol:** when Clyde says "catch up" (in any AI tool), that means: read this entire file, then read the most recent entries in "Session Log" until you understand what has changed since the last session, then summarise back in 2-4 sentences what state the project is in and what you'd do next — before taking any other action or asking what to do.

**Writing rules for this repo — this repository is PUBLIC. Apply these to every file, including this one:**
- **Never name an employer, client, or any internal/commercial system.** This project stands on its own and has no relation to any employer's work.
- **Never name a specific AI tool or vendor**, in docs or in commit metadata. No AI assistant is credited as an author or co-author of any commit. Session Log entries record *what changed*, dated — not which product made the change. **This includes machine-added `Co-Authored-By:` trailers — AI coding tools append them to commit messages by default, and GitHub turns them into a visible "contributor" on the repository.** This happened once: two pushed commits carried such trailers until 2026-08-11, when the history was rewritten and force-pushed clean. Every session that commits must strip any auto-added trailer, and before any push run `git log --format='%B' | grep -i co-authored` — it must return nothing.
- Keep prior-art framing honest (see "What this is explicitly NOT").

---

## Project summary

A scheduler that decides which GPU runs which job, and when, based on two combined factors:

1. **Hardware efficiency profile** — different GPU types have different power-to-throughput curves. The scheduler allocates jobs to the GPU best suited to the job's power/performance profile rather than treating all GPUs as interchangeable.
2. **Grid signal timing** — using live or forecast electricity price and/or carbon intensity data, the scheduler shifts flexible (non-urgent) workloads to run when the grid is cheaper or cleaner. Urgent jobs run immediately regardless of grid state.

The system must be **market-agnostic**: the scheduling logic never touches market-specific code directly. Each electricity market (GB, CAISO, ERCOT, etc.) has its own adapter that translates that market's API into one common data format. Switching markets should be a one-line config change, not a code change.

## What this is explicitly NOT

- **Not a carbon credit generator.** Carbon credits/RECs are certified instruments issued by accredited bodies against verified, additional emissions reductions. This software does not create tradeable credits. It reduces actual energy cost and can improve hourly carbon-matching metrics. Never describe it as generating credits in any write-up or pitch.
- **Not a novel category.** It is an applied synthesis of existing published mechanisms (see Prior Art below). Present it honestly as a combined implementation, not an invention.

## Prior art (read before building — do not reinvent unknowingly)

| System | What it does | Source |
|---|---|---|
| Zeus (NSDI '23) | Optimises GPU power limit + batch size to minimise training energy | https://www.usenix.org/system/files/nsdi23-you.pdf |
| Perseus (PyTorch/SymbioticLab) | Extends Zeus to large-scale/pipeline training | https://arxiv.org/pdf/2312.06902 |
| Google Carbon-Intelligent Computing | Shifts compute timing/location to match grid carbon intensity | https://arxiv.org/pdf/2106.11750 |
| GridCensus / LandGate / REST | Data centre *siting* tools (different problem — where to build, not how to run) | gridcensus.com |
| **Compute Gardener** (open source, active, 2025-2026) | A real Kubernetes scheduler plugin doing almost exactly this project's two pillars combined: carbon + time-of-use-price-aware temporal shifting (pre-filter stage) AND hardware power-profile-aware node placement, incl. GPU workload classification (filter stage). Uses Electricity Maps API for grid data, NVIDIA DCGM + Kepler for hardware power estimation. Founder (Dave Masselink) is actively seeking production validation partners as of Aug 2025. | https://github.com/elevated-systems/compute-gardener-scheduler , https://compute-gardener.com |
| **Eco-Orchestrator / CARL** (academic, 2025-2026) | Combines carbon-aware job placement (grid forecasts) with hardware optimisation (automated DVFS on NVIDIA A100s) via a reinforcement-learning scheduler. Tested on a 64-A100 Kubernetes testbed; reported 34.7% carbon reduction. | found via arXiv/search, exact paper not yet pinned down |
| Green Software Foundation Carbon-Aware SDK, Azure `carbon-aware-keda-operator`, Kepler (K8s power exporter) | Mature, widely-adopted open-source building blocks for carbon-aware infra — not a full scheduler on their own but the standard plumbing everyone in this space uses | greensoftware.foundation, github.com/Azure/carbon-aware-keda-operator, sustainable-computing.io |

**Correction to the gap claim (2026-08-09):** the original claim — "nobody has done both together openly" — is **not accurate** and must not be repeated in any write-up or pitch. Compute Gardener already ships both pillars (grid-signal timing + hardware-aware placement) as an open-source, actively-developed project, and Eco-Orchestrator/CARL demonstrates the same combination academically at real GPU-cluster scale (64 A100s). This space is more mature and more validated than initially scoped. See "Where the real gap might still be" below for what's left.

## Where the real gap might still be (revised 2026-08-09)

Given the above, straight prior art re-implementation is not a credible pitch. What still looks open, and should be validated (not assumed) before claiming it:

- **On-device / Apple Silicon**: everything found so far (Compute Gardener, Eco-Orchestrator, Zeus/Perseus) targets Kubernetes clusters or discrete NVIDIA GPU fleets. Nobody found yet targets heterogeneous on-device compute (CPU P/E-core, GPU, ANE) paired with consumer time-of-use tariffs. See "Possible platform target: Apple Silicon companion demo" above.
- **Live hardware auto-detection + empirical benchmarking** rather than relying on DCGM/Kepler power estimates or hand-curated specs — worth checking directly against what Compute Gardener's "hardware power profiling" actually does before claiming this as new; it may already do something similar.
- Honest framing going forward: this project should be positioned as *implementing and extending the Compute Gardener / Eco-Orchestrator pattern*, ideally with a genuinely uncovered angle (Apple Silicon), not as filling a gap nobody has touched. Worth actually reading Compute Gardener's source/docs before writing any more scheduler code, to avoid rebuilding it blind.

## Architecture decisions already made

- **Adapter pattern**: one `MarketAdapter` interface. Each market (GB, CAISO, ERCOT) gets its own adapter file translating that market's API into a common format: a timestamped series of `{timestamp, carbon_intensity, price}`.
- **Scheduler logic is fully decoupled** from any market-specific code. It only ever sees the common data format.
- **GPU hardware profiles** are a separate input entirely — real published power/throughput specs, not invented numbers.
- **Build order**: get ONE market (GB) working end to end first. Do not build all three adapters before the core pipeline works. Add CAISO/ERCOT only once the interface is proven.

## Current state

*(Update this section at the end of every session — replace this line with what was actually done)*

- **Now a git repository** (initialised 2026-08-09). The National Grid Tool is deliberately NOT part of it — see "External dependency" below.
- **Environment set up and working on Clyde's Mac.** Python 3.12.9 venv at `~/venvs/national-grid` (outside the project folder, per the National Grid Tool README's own convention), installed from that project's `requirements.txt`. Run anything in this project with `~/venvs/national-grid/bin/python`.
- **`gb_adapter.py` is now VERIFIED LIVE**, not just against fixtures. Real run for 2026-08-01 → 2026-08-07 returned 289 `GridDataPoint`s with 0 missing prices; carbon 31–186 gCO2/kWh, and (see bug below) real day-ahead price £2.84–£158.94/MWh. The carbon/price join holds against live data.
- **Speed measured:** ~0.3s per day of data fetched (7 days = 2.14s). 92% of that is the Carbon Intensity endpoint, which is one HTTP call *per day*, vs prices which chunk 5 days per call. A full year is therefore ~2 minutes of live fetching. Fine once, wasteful on every scheduler iteration — see next steps.
- **KNOWN BUG, deliberately NOT fixed yet** (will be handled in the real implementation rather than patched into the current adapter): `gb_adapter.get_data()` averages Market Index price across data providers, but only two exist and **`N2EXMIDP` publishes structural zeros** — 100% zero in Aug/Jun/Feb 2026, 99.6% zero in Nov 2025. So every price the scheduler currently sees is **exactly half the real price** (measured mean £57.44 vs true APX mean £115.00). The level error alone understates savings 2×; worse, the Nov 2025 case means the scale factor is 0.5 for most periods and something else for a few, which distorts the *shape* of the price curve — and shape is the entire input to a "which half-hour is cheapest" decision. **Fix when rebuilding: prefer `APXMIDP`, or drop non-positive prices before averaging. Do not average blindly.** This resolves open question #2 below.
- `adapters/base_adapter.py` done — `MarketAdapter` interface + `GridDataPoint` dataclass.
- `adapters/gb_adapter.py` is now a REAL implementation, not a stub. It imports a sibling project, `AI Energy/National-Grid-Tool` (Clyde's own earlier GB power-market data project — 28 fetchers, SQLite+Parquet warehouse), and calls two of its clients directly: `sources/carbon_intensity/client.py` (Carbon Intensity API, public/keyless) and `sources/elexon/prices.py` (Elexon Insights Market Index Data, public/keyless).
- Signal choice made (not yet validated on live data): uses `forecast_gco2_kwh` (not settled `actual`) and day-ahead Market Index price (not settlement system price), because a real scheduler only ever has forward-looking data when it decides — see the docstring at the top of `gb_adapter.py` for full reasoning. This is a judgment call, open to revisiting.
- Join logic (settlement_date + settlement_period, averaging price across multiple Market Index providers) was verified offline against the National Grid Tool's own real test fixtures (`tests/fixtures/carbon_intensity_date.json`, `tests/fixtures/mid_2026-07-13.json`) — mechanically correct.
- ~~NOT yet verified: an actual live network call.~~ **Superseded — the live call was made and passed on 2026-08-09.** (This line previously contradicted the "VERIFIED LIVE" bullet above and misled a later session into re-running work that was already done. Left visible rather than deleted, so the contradiction is a matter of record.)

**Built 2026-08-09 (session 5) — the first working vertical slice:**
- `adapters/gb.py` — **the corrected GB adapter, and the one to use.** Same signal choices, but price comes from a single preferred provider (`APXMIDP`) with non-positive prices dropped, instead of a mean across providers. Returns `GBPoint`, which carries `price_provider` and `settled_carbon` so every figure's provenance travels with it. `gb_adapter.py` is untouched and superseded — keep it for the record, don't build on it.
- `core/grid.py` — **market-agnostic** scheduling algorithms and energy accounting. Knows nothing about GB. `Job`, `run_immediately` (naive baseline), `cheapest_window`, `cleanest_window`, `compare`. The allocator is a sliding-window minimum: no solver, no ML, microseconds to run.
- `app/dashboard.py` — generates a self-contained HTML dashboard from live data. **Not Streamlit** (see "UI and deployment" below).
- `tests/test_grid.py` — 15 offline tests, all passing, no network. Covers the accounting, deadline enforcement, inflexible jobs, and the rule that unpriced windows are never chosen (a window with missing data is not a cheap window).
- **Real measured result** (GB, 2026-08-05→08, 6.5 kW job for 4 h, 24 h deadline): baseline £3.21 / 2.10 kgCO₂ → scheduled £0.21 / 0.97 kgCO₂. **93.6% cost and 54.0% carbon saved for an 11.5 h delay.** Verified against live data, with the corrected price path. Steps 5, 6 and 7 are substantially done.
- Confirmed on real data that **cost-optimal and carbon-optimal windows can disagree**, sometimes with the cleanest window costing *more* than running immediately. The scheduler has to be told which objective it is serving; it cannot infer it. The dashboard surfaces this only when the trade-off is material (≥2% cost penalty), because a bare index comparison fires on windows a single period apart that cost the same to a penny.
- No GPU profile data collected yet — hardware detection (step 4) is the remaining gap, and now the only thing standing between this and the full pitch.

**In progress 2026-08-09 (session 6, checkpoint 1):**
- `core/planner.py` now contains an exact, auditable multi-objective placement engine. It exhaustively evaluates every feasible hardware, location and half-hour start combination; enforces memory and deadline constraints; applies PUE to facility energy; supports explicit cost, carbon and delay weights; and marks the cost/carbon Pareto frontier. It does not use an opaque model.
- `adapters/caiso.py` adds the first US power market. It joins official CAISO OASIS day-ahead locational marginal prices with EIA-930 hourly CAISO fuel mix. Six hubs/load aggregation points and validated custom PNodes are supported. Price precision is nodal; carbon is deliberately labelled as a CAISO balancing-area production estimate with imports excluded, never as nodal carbon.
- `adapters/nyiso.py` adds a second independent US power market. It joins official keyless NYISO day-ahead zonal LBMP files with EIA-930 NYISO fuel mix. Eleven New York zones are selectable. Price is zonal; carbon remains explicitly balancing-area scoped.
- `app/markets.py` centralises market/location semantics. GB exposes fourteen selectable distribution regions whose carbon changes while price remains national. CAISO exposes node-level price locations. Both products use a recent complete historical replay where a complete forward price curve is unavailable rather than pretending historical data is a forecast.
- `app/planner.py` is now the third integrated product page at `/planner`. It searches the full hardware catalogue and all legal half-hour starts at a selected facility location, exposes cost/carbon/delay weights, PUE, system efficiency, memory mode, context and batch, shows a decision trace, ranked alternatives and the Pareto frontier, and stores configuration in the URL.
- `app/simulator.py` now models facility PUE, measured system efficiency, training state sharding versus replication, and inference KV-cache memory from model architecture, context length and concurrent sequences. Scenario configuration persists in the URL and hardware selection is grouped by vendor.
- Every analytical chart now opens into a real full-screen workbench rather than scaling a static SVG. The seven compact Grid panels and Planner Pareto chart support independent X/Y zoom, pan, shift-drag box selection, pointer/touch input, crosshair values, series visibility, grid/log toggles, exact-data tables, CSV and PNG export. Simulator renewable data uses the same engine; its allocation diagram has a separate pan/zoom/SVG workspace. The main price and carbon charts preserve all range, interval, candle and overlay state in full screen, add denser horizontal and vertical grids, a labelled latest-value level, exact axis crosshair badges and independent Y zoom/pan.
- `app/serve.py` now routes five linked operator surfaces and keys its five-minute market-data cache by market and location. `/` is AI Operations; `/simulator` is Fleet Lab; `/planner` is Placement Lab; `/grid` is the Sites & Grid terminal; `/decisions` is the evidence journal. The first four preserve `?market=GB|CAISO|NYISO&location=...`; CAISO also accepts a validated custom PNode. `/workloads` remains a compatibility alias for AI Operations.
- Market/location state now travels through Grid, Simulator and Planner. The simulator uses the selected market's currency, price source and balancing-area carbon scope; CAISO SP15 was verified live through `/simulator?market=CAISO&location=sp15`.
- CAISO carbon now prefers EIA-930's published consumption rate when available and falls back for recent periods to a known-fuel production estimate. Unclassified "other" generation is excluded from both numerator and denominator rather than treated as zero-carbon; imports remain excluded in fallback mode. The active method is visible.
- Planner hardening now rejects timestamp gaps inside a run, refuses to rank mixed currencies without an explicit conversion and calculates the Pareto frontier in `O(n log n)` rather than pairwise quadratic time.
- Optional hard policy caps now constrain maximum cost, carbon and start delay before multi-objective normalisation. API numeric inputs reject non-finite values, boolean counts and unsafe dimensions rather than allowing unstable rankings or unbounded work.
- `docs/planner.md` now states the exact equations, constraints, normalisation, tie-breaking, grid precision, primary data sources, computational complexity and limits before production use.
- `hardware/providers.py` now supplies the planned provider boundary. `LocalDetector` reads Apple identity/memory or NVIDIA identity/memory/live power without retaining host serials or UUIDs; `SimulatedFleetProvider` loads a catalogue-backed JSON fleet. Per-field provenance prevents measured identity from making estimated throughput look measured. The current Mac is detected as one Apple M2 with 8 GB measured memory and estimated performance/power.
- `hardware/calibration.py` now validates repeated empirical observations and builds exact-fingerprint profiles. At least three runs must match device, model, task, precision, accelerator count and software stack. Planning uses robust median throughput and IT power, carries variation in its assumptions, and falls back to ESTIMATED on any mismatch.
- `core/estimator.py` is now the canonical server-side workload estimator. It models topology scaling, measured system efficiency, PUE, 16- or 8-byte training state, activation/buffer reserve, replication versus sharding, and independently configured KV-cache precision. It builds real `PlanningCandidate`s and calls the exact planner.
- `core/routing.py` corrects the earlier task-difficulty premise. Semantic difficulty does not directly change hardware demand for a fixed model; it establishes a measured quality floor. The router first filters versioned model deployments by measured quality, then optimises eligible deployments across hardware, location and time. It never invents quality scores.
- A versioned local JSON contract now exists: `GET /api/v1/health`, `GET /api/v1/market`, `POST /api/v1/plan` and `POST /api/v1/portfolio`. Market data is shared across page/API caches. Single-plan responses include full inputs, provenance, selected placement, Pareto status, ranked alternatives and rejection reasons. Portfolio responses include quality-filtered queue assignments, facility capacity, completed work by unit, energy, cost, carbon, exactness and search bounds. Bodies are capped at 64 KiB.
- The planner page now exports a shareable review URL, plan JSON and alternatives CSV.
- `core/backtest.py` now preserves a forecast-time decision and scores it on a separate realised series. It reports realised savings against immediate execution, forecast error and perfect-hindsight regret without leaking the oracle into scheduling. API forecast snapshots are now persisted; live outturn ingestion is not wired yet.
- `core/audit_store.py` now persists every API planning request, exact response and complete decision-time signal snapshot under a UUID in local SQLite. Decisions can be listed/retrieved and a later realised backtest score can be attached. The API exposes `/api/v1/decisions` and `/api/v1/decisions/{id}`.
- The planner's "Save audited decision" control now calls the canonical server-side plan API rather than treating the browser calculation as authoritative. A later outturn can be submitted to `/api/v1/decisions/{id}/score`; the original decision remains immutable while the realised score is attached separately.
- `/decisions` is now the linked operator decision journal. It loads compact summaries from the audit API, searches and filters by evidence state, opens the exact stored constraints, selected placement, provenance and realised score, and exports the immutable record as JSON.
- All five pages now carry one persistent top-right Light/Dark slider from `app/theme.py`. The stored preference is shared across navigation and explicitly overrides the operating-system theme in both directions.
- `scripts/launch.py` plus the generated Desktop `Grid-Aware Scheduler.app` provide one-click startup. The launcher reuses a healthy local process or selects a free loopback port, starts the server, waits for its health contract and opens the product without a terminal window.
- HTTP responses now carry no-store, nosniff, frame denial, no-referrer and a restrictive same-origin content security policy. JSON writes require the correct content type and bodies remain capped at 64 KiB.
- The project-environment test suite is currently **163 passing**. It includes generated JavaScript parsing, persistent theme controls, full-screen chart workbenches, exact workflow/dependency scheduling, checkpoint chunks, physical multi-source energy dispatch, storage, counterfactual reporting, exact site/source coordinates, distance and declared delivery-loss accounting, governed workload evidence, API contracts and local HTTP/security checks.
- **Current local runtime:** product version 0.11.0 is managed by the Desktop launcher at `http://localhost:8766`, PID 8260. Health reports API `v1` and `local_only: true`. Live Operations verification produced the complete default schedule, exposed the empty measured-profile registry truthfully, exercised full-screen grid/exact-data controls, closed back to the compact card, and followed the Sites & Grid link. This is loopback-only, not a deployment.
- Product direction agreed on 2026-08-10: the system is a joint AI-workload and grid-signal optimiser. Workload telemetry estimates energy, duration, memory, quality and useful work for each execution option. Time- and location-specific electricity price and carbon intensity then determine cost and emissions. Grid prices remain a first-class scheduling signal; AI profiling does not replace market data.
- Information architecture correction agreed after 0.7.0: the product must open as an AI data-centre operations and optimisation system. Workload demand, service quality, fleet capacity, deadlines, execution variants and recommendations come first. Grid price/carbon are critical site-and-time placement inputs but the detailed grid terminal is a secondary specialist view, not the landing identity.
- Primary user: an operator scheduling AI data-centre workloads. The Apple Intelligence, Private Cloud Compute, Apple silicon and Core ML context is the first demonstration, while workload and fleet contracts remain architecture agnostic.
- Eight-day evidence target: benchmark a representative workload registry on the available M2 Mac and, where practical, the user's iPhone and iPad; retain only privacy-preserving measurement metadata; connect those measurements to the existing regional price/carbon scheduler and operator dashboard.
- Local measurement environment: Xcode and `xctrace` are available. MLX 0.32.0, MLX-LM 0.31.3, Core ML Tools 9.0 and psutil 7.2.2 are installed in the configured environment. The iPhone is identified; exact iPad model/OS remains to be recorded.
- Apple-device discovery now identifies an iPhone 16 Plus on iOS 26.5, paired but offline. No physical iPad is paired, so its exact model and operating-system version remain an input from Clyde. The M2 Mac has about 17 GiB free; the initial model-weight budget is capped at 2 GiB.
- Optional profiling dependencies are pinned in `requirements-apple.txt` and installed. No model weights or evaluation data have been downloaded.
- `docs/apple-measurement-protocol.md` defines the evidence hierarchy and run controls. Apple subsystem power estimates may support same-Mac configuration optimisation but must not be used for cross-device energy comparison. Mac/iPhone/iPad comparisons require a calibrated wall or supported battery-energy measurement; energy-impact scores are not joules.
- `core/evidence.py` now provides a modality-neutral, metadata-only `workload-evidence-v1` contract. It supports tokens, images, audio seconds, samples, training examples and optimiser steps; requires versioned measured quality; aggregates at least three exact-fingerprint runs with robust throughput/energy variation; and converts a measured profile into the existing grid-aware planner without storing workload content.
- `core/evidence_store.py` is an append-only local SQLite registry. Immutable exact-fingerprint observations become a stable profile only after three valid runs. The API lists profiles and ingests observations; the portfolio endpoint derives scheduling fields from a selected stored profile and rejects conflicting browser values.
- `hardware/apple_benchmark.py` is the executable Mac collector. It measures duration and peak process-tree memory, consumes only metadata-only useful-work/quality output, and requires either authorised Apple subsystem sampling or calibrated external-meter watt-hours. Its MLX probe confirms local execution but deliberately creates no scheduler profile without quality and energy evidence.
- AI Operations now lists governed profiles and observation counts. Selecting a profile replaces editable model, runtime, power, memory and quality fields with server-derived measurements. Empty state and same-device energy comparison limitations are visible.
- `core/portfolio.py` now provides the first multi-job operator algorithm. It enforces half-hour facility power capacity, deadlines and hard total cost/carbon budgets; schedules mandatory and optional jobs; maximises explicit operator utility; then minimises carbon, grid cost and delay. Unlike work units remain separate. The bounded pilot search is exact and fails closed above its declared combination limit.
- **Git state correction (2026-08-10, discovered session 23):** the working tree had drifted from what the log implied. A single commit `a474b20` ("Build grid-aware AI operations control plane", 2026-08-10 11:53:15 +0100) had already been made and pushed to `origin/main` outside any logged session — it bundles everything through session 16 (planner, CAISO/NYISO, hardware providers/calibration, estimator, routing, audit store, backtest, decisions journal, theme, launcher). It does **not** include `core/energy.py` (session 17 onward) — generation-aware dispatch, geospatial siting, the Apple-evidence pipeline (`hardware/apple_benchmark.py`, `core/evidence_store.py`) and automatic model routing were still local-only as of session 23. If a future session finds commits it didn't make, check `git log` before assuming the doc is authoritative — the doc had genuinely fallen behind reality once already.
- **Visual/product redesign started (session 23, 2026-08-10/11).** Clyde's direct feedback: the UI "looks clearly done by codex" — specifically the shading/hover style, not the underlying Apple-esque colour tokens (those stay). Concrete asks: (1) restyle away from the generic dark-dashboard tell (pure pill-everything nav/buttons, uniform bordered-box cards, shadow-pop hover, uppercase kicker labels above every section) while keeping the Apple palette and the vibrancy/blur that already works; (2) bring charting up to "Yahoo Finance level" — proper crosshair, more chart types, more financial tools, not just headline-grabbing histogram/heatmap ("loads you could add"); (3) reduce information density — Operations queue cards read as "a dump of info on top of info". Directive: keep working until tokens run out, keep HANDOFF current at every checkpoint so no session loses the thread.
- `app/chart.py` **rebuilt** with real financial-terminal tools, verified by rendering the chart standalone in headless Chrome and screenshotting each mode (not just `node --check`): **Baseline, Histogram and Heatmap chart types** (Histogram = distribution of the series across the selected range with P10/median/P90 reference lines; Heatmap = hour-of-day × weekday seasonality grid, colour-mixed in the series' own hue so it stays theme-consistent); **Bollinger Bands** overlay (SMA ± 2σ); **log-scale Y** and **% change from range start** toggles (mutually exclusive, each disables the other when it would be meaningless — log needs a positive floor, so it self-disables under a negative/zero low or under %Δ); a **floating cursor tooltip** with full OHLC next to the pointer (previously the OHLC readout only lived in the fixed header); toolbar de-pilled (nav/segment radii 980px→8-10px, matching the broader restyle below).
- **Real bug found and fixed while building the above, not by inspection but by screenshotting an actual interaction:** SVG elements' `.hidden` IDL property does not reliably reflect to the content attribute in current Chrome (confirmed empirically: `el.hidden = false` left `getAttribute("hidden")` unchanged) — same family as the already-documented "SVG ignores the HTML `hidden` attribute" issue, but one layer deeper: even the JS-side toggle can silently no-op. Symptom caught: switching to Heatmap left the previous mode's "latest value" badge and dashed line stuck on screen. Fixed everywhere in `chart.py` by replacing every `el.hidden = x` with a `setHidden(el, x)` helper that calls `setAttribute`/`removeAttribute` directly. **This pattern (`el.hidden = x` on an SVG/foreign element) should be treated as unsafe project-wide** — `app/panels.py`, `app/dashboard.py`, `app/flow.py` and `app/kline.py` were not audited for the same bug this session; worth a targeted grep (`\.hidden\s*=`) next session if any toggle-hide behaviour on those pages looks stuck.
- **Redesign completed (session 23, same sitting).** Superseding the "in progress" note above: the restyle and density pass finished. Carried the chart.py de-pill treatment into `app/dashboard.py`, `app/panels.py`, `app/planner.py`, `app/simulator.py`, `app/workloads.py`, `app/decisions.py` — every page nav is now a real segmented control (track + raised on-state chip, matching the chart toolbar) instead of a row of separately pill-shaped links with a solid-fill active state; `.pnl:hover` (panels.py, shared by every analytics panel) moved from a drop-shadow lift to a background tint + inset ring; every "kicker label above a headline" (`.eyebrow`, `.ops-stat span`, `.chain-step span`, `.pnl h3`, `.slot h3`, `.metric span`, `.comparison-stat span`, `.section h4`) had `text-transform:uppercase` and wide letter-spacing removed in favour of a plain sentence-case secondary label — deliberately **not** touched: table `<th>` headers and toolbar micro-labels (`.ch-lbl`, "Range"/"Interval"/"Type") stay uppercase, and status/provenance tags (`.badge`, `.pill`, `.evidence-state`, `SPEC`/`MEASURED`/`ESTIMATED`/`Pareto`) keep both their pill shape and caps — those are genuine conventions, not the tell. The Operations `.decision-chain` step row (previously 5 individually bordered chips with a floating `>` glyph between them) is now one divider-grid strip, matching the existing `.ops-grid`/`.metrics` tile pattern already used elsewhere on the same page rather than inventing a new component.
- **Operations queue-item density fix:** each job card's ~28 flat, identically-styled fields (previously one undifferentiated grid — the literal "dump of info on top of info") are now five `<details>` groups — Identity & workflow, Execution, Quality & schedule (open by default), Resources, Evidence (collapsed by default) — using native disclosure triangles, no JS added. `data-field` lookups are unaffected since `querySelector` reaches into closed `<details>` content regardless of open state; verified the existing `collect()`/`bindRemove()` JS still works by re-running the full suite and by screenshotting the rendered page with sections both open and collapsed.
- All of the above verified in both directions before moving on, not just "looks right": (1) `tests/` full suite — **172 passing**, unchanged from before the redesign; (2) `python -m py_compile` on every `app/`, `core/`, `adapters/`, `hardware/` module — clean; (3) `git diff --check` — no whitespace errors; (4) rendered every page through the real local server and screenshotted Operations, Grid, Fleet Lab (Simulator) and Placement Lab (Planner) in dark, and Operations again in light (via a saved-HTML + forced `data-theme` trick, since headless Chrome's CLI screenshot mode does not expose a `prefers-color-scheme` override flag) — chosen deliberately because no chart or panel color value was touched this session, only shape/spacing/typography, so light-mode risk was low and one confirmed page was judged sufficient rather than screenshotting all five twice.
- **Scope deliberately cut, not forgotten:** PNG export for the main chart terminal (`app/chart.py`) was designed but not built — panels.py's analytics workbench already has PNG export, so the product isn't without it, but the terminal itself only has CSV. Flagged as a next-session pickup, not a silent gap.
- **Facility hardware auto-discovery designed, not built (2026-08-11).** `docs/discovery.md` defines the discovery boundary before any collector code, per item 19's own instruction. Three access tiers: tier 0 is anonymous-by-standard reads only (Redfish service root under DSP0266, passive LLDP listening — and explicitly *not* SNMP `public`, because a community string is a credential and trying defaults is credential guessing); tier 1 is operator-supplied read-only credentials (authenticated Redfish inventory, SNMP v2c/v3 for PDU/UPS, DCGM as credentialed-equivalent since it has no auth of its own, IPMI as legacy read-only fallback); tier 2 is never-automatic (any write/action, credential guessing, scans outside declared endpoints, retention of raw serials/UUIDs/MACs). Other load-bearing decisions: endpoints are operator-declared — enumeration, never subnet scanning; device dedupe uses an HMAC-SHA256 keyed digest of the protocol-native ID with a local site key, so fleet identity is stable without retaining raw identifiers (extends the existing `LocalDetector` privacy rule to fleet scale); every discovered power figure carries a measurement scope (`board`/`system`/`outlet`, mirroring `workload-evidence-v1` energy-method scopes) so a PDU outlet reading is never compared to a GPU board reading as if equal; discovery can never promote performance above catalogue provenance — calibration stays the only path to MEASURED throughput/power curves. **Redfish is proposed as the first collector**, selected by the same logic that picked GB as the first market adapter: inventory is the protocol's designed purpose, the anonymous/credentialed boundary is defined by the standard itself, it needs zero new dependencies (stdlib HTTPS+JSON; `ipmitool`/`lldpcli`/`dcgmi` are all absent from this machine, checked), and it has an honest verification ladder without a data centre — DSP2043 mockup fixtures → local HTTP serve → OpenBMC under QEMU (real BMC firmware) → physical BMC. No physical BMC/PDU/switch/NVIDIA GPU is reachable from the current dev machine, so stage 4 waits on real hardware access.
- **`RedfishFleetProvider` built and verified through ladder stage 2 (2026-08-11, same day).** Clyde approved proceeding past the design review. The provider lives in `hardware/providers.py` beside `LocalDetector`: GET-only walks of operator-declared endpoints; anonymous requests limited to the DSP0266 service root (credentials are never sent to it, and without credentials the walk stops there with a warning); HTTP Basic from an environment variable for the credentialed descent through Systems → accelerator Processors (CPUs stay summarised on the system record) → Chassis → Power, joining chassis input watts to systems strictly via `PowerControl.RelatedItem`, never by guessed paths; per-endpoint request budget that fails closed to a partial inventory; TLS with optional per-endpoint certificate-fingerprint pinning, plain HTTP rejected off loopback at construction time; `DetectedDevice` gained `power_scope` ("board"/"system"/"outlet") and `device_digest` (HMAC-SHA256 keyed digest under a site key auto-created at `data/cache/discovery-site-key`, gitignored — raw serials/UUIDs/SKUs/asset tags are discarded at read time and asserted absent from `public_dict()`). Fixtures are a pruned copy of DMTF's real `public-rackmount1` mockup tree (`tests/fixtures/redfish/`, BSD-3-Clause, copyright retained, README records the source) — the same verify-against-upstream's-own-fixtures pattern as `gb_adapter`. Nine new offline tests cover the full walk, identifier hygiene, digest stability/keying, both no-credential paths, the loopback rule, the request budget and multi-endpoint partial failure. **Ladder stage 2 verified live:** the provider's real `http.client` transport walked the full mockup tree served by a stdlib loopback shim that enforces 401 below the service root — inventory returned Contoso 3500 (96 GiB MEASURED memory, 344 W MEASURED system-scope power, performance UNAVAILABLE) plus the Stratix 10 FPGA (identity MEASURED, everything else UNAVAILABLE), zero warnings, zero identifier/credential leaks. Stages 3 (OpenBMC/QEMU) and 4 (physical BMC) remain open.
- **Discovery now feeds the product (2026-08-11, same sitting).** `hardware/inventory_store.py` is an append-only SQLite snapshot store under the ignored `data/cache/` path (record/latest/summary only — no update or delete exists, and a test asserts that). `GET /api/v1/inventory` reports the latest snapshot, store summary and whether discovery is configured; `POST /api/v1/inventory/refresh` walks the declared endpoints read-only and records one snapshot (409 without config, 201 with the snapshot). Config lives at `data/discovery.json` (`facility-discovery-v1`, gitignored, `DISCOVERY_CONFIG` env override). Operations gained a "Discovered fleet inventory" card between the evidence registry and energy supply: snapshot/device/warning counts, a per-device table with per-field provenance, power scope, source endpoint and short keyed-digest fleet ID, a Run discovery button, and honest empty states for both unconfigured and configured-but-never-run. Verified offline (**187 passing**, +6: three store tests, three server tests including a real-loopback-HTTP discovery walk against the DMTF fixture tree with an auth-enforcing handler and an end-to-end identifier-leak assertion) and live (real server + mockup shim: 409→201→snapshot flow over curl, then headless-Chrome screenshots of the rendered card — which caught a five-column stats grid holding only four tiles; fixed, server restarted, re-screenshotted clean). The QA-generated `inventory.sqlite` and site key were deleted afterwards so no fabricated device record persists, per the session-6 audit-store precedent.

## Next steps (in order)

1. [x] Correct GB price ingestion, cache historical data, and ship the first measured grid dashboard.
2. [x] Build the hardware/model simulator and catalogue, including topology-aware scaling estimates and provenance.
3. [x] Build an exact joint hardware/time/location planning engine with hard memory/deadline constraints, PUE and explicit multi-objective weights.
4. [x] Add CAISO day-ahead nodal pricing plus clearly scoped EIA balancing-area carbon estimates; expose GB regional carbon and CAISO pricing locations through one market context.
   NYISO zonal pricing and balancing-area carbon are also complete. ERCOT, PJM, MISO, SPP and ISO New England remain separate adapters, not one generic US signal.
5. [x] Connect `/planner` and market/location query state in the local server, update the grid dashboard for GB/CAISO currency and provenance, and link Grid, Simulator, Planner and Decisions in both directions.
6. [x] Extend generated-page tests for planner JavaScript, accessible chart expansion, navigation, market controls and URL state; run the full suite and local HTTP smoke tests.
7. [x] Write the operator-facing algorithm and data-contract explainer in `docs/`, update the README, and record the verified live CAISO result.
8. [ ] `HardwareProvider` plus controlled exact-fingerprint calibration are built. **CURRENT:** add trusted automatic telemetry collection on supported hosts and accumulate real calibration runs so local profiles can replace estimates.
9. [ ] The market-agnostic backtest engine and decision-time signal persistence are built. **CURRENT:** ingest realised price/carbon and trigger scoring automatically. CAISO forecasting remains a separate later layer from the current historical replay.
10. [ ] Local durable persistence is built. **CURRENT:** add authentication and authorisation, tenant isolation, workload execution integration, observability, reliability controls and deployment packaging before describing the product as production-ready.
11. [x] Replace enlarged static charts with reusable full-screen analytical workbenches and bring the primary time-series charts up to operator-grade interaction. Browser QA completed for Grid compact panels, main market charts and the Planner Pareto chart; all generated JavaScript also passes parser tests.
12. [x] Expose the joint workload/energy optimisation contract through the versioned API and AI Operations interface, including facility policy, source dispatch, checkpoint chunks, per-unit outputs and an earliest-run counterfactual.
13. [ ] Add an extensible benchmark registry with reference workloads for language-model inference, lightweight fine-tuning, vision and speech. Treat broader AI modalities as plug-ins rather than claiming that the first evidence set covers every workload.
14. [ ] The reproducible Mac runner and pinned environment are built. **CURRENT:** add governed reference workload plug-ins and collect valid repeated Mac runs, then build a minimal native iPhone/iPad collector where device support permits. Do not store prompts, media or model inputs in shared telemetry.
15. [ ] Stored measured profiles now feed the portfolio scheduler and replace conflicting browser estimates. **CURRENT:** accumulate real profiles, add multiple execution variants per workload, and automate predicted-versus-realised energy, cost and emissions reconciliation.
16. [ ] Complete an eight-day evidence campaign and publish a reproducible case study. Keep the public repository free of proprietary workloads, private data and unsupported Apple deployment claims.
17. [x] Add exact operator-declared facility/source coordinates, connection identities, audited distance and declared delivery losses while keeping provider price/carbon scope separate. CAISO accepts exact published PNodes; GB and NYISO retain their truthful regional/zonal boundaries.
18. [ ] Replace scenario geometry and losses with operator GIS, meter, interconnection and network-study data; add supported market adapters before optimising a site outside GB, CAISO or NYISO.
19. [ ] Facility-scale hardware auto-discovery: extend `hardware/providers.py`'s single-host `LocalDetector` into a fleet-wide provider that inventories everything connected in a facility — servers/BMCs via Redfish or IPMI, network topology via LLDP, PDU/UPS via SNMP, GPU fleet identity and live power via NVIDIA DCGM — instead of requiring a manually typed fleet file. This closes the gap between "hardware auto-discovery tools exist" and "grid-aware scheduling exists" — nothing today connects the two into one pipeline. **CURRENT (2026-08-11):** `docs/discovery.md` defines the boundary; `RedfishFleetProvider` is built and verified through ladder stage 2; and discovery now feeds the product — append-only `hardware/inventory_store.py`, `GET /api/v1/inventory` + `POST /api/v1/inventory/refresh`, and the Operations "Discovered fleet inventory" card, all verified offline and live. Next: ladder stage 3 (OpenBMC under QEMU), then stage 4 on a physical BMC when hardware is reachable; DCGM → SNMP-PDU → LLDP collectors, each with the same tier analysis first; and letting a discovered, catalogue-matched accelerator flow into portfolio device scope the way governed evidence profiles already do.
20. [ ] **Directed 2026-08-11.** Comprehensive US market coverage — CAISO and NYISO were only two of seven US ISO/RTOs. **CURRENT: MISO is done and verified live** (see Current State). Reachability was measured before choosing, and it reordered the plan: ERCOT's MIS times out entirely from this UK network (geo-blocked) and both `www.ercot.com` and `api.ercot.com` return 403 — the new public API needs a free registered subscription key, **a blocking input only Clyde can supply**. Confirmed 2026-08-11 in a real browser, not just curl: even the registration portal `apiexplorer.ercot.com` serves an Imperva/Incapsula "Access Denied, Error 16" page to Clyde's UK IP, so ERCOT registration itself requires a US network path (a US-exit VPN, or waiting until back on a US connection — from New York it should work directly); MISO's `docs.misoenergy.org` market reports answered 200 keyless immediately; SPP's `portal.spp.org` was re-probed with 45-second timeouts and times out at the connection level exactly like ERCOT MIS — also geo-blocked from the UK despite being keyless in principle. PJM measured the opposite way: `dataminer2.pjm.com` and the registration portal `apiportal.pjm.com` both return 200 from the UK, and `api.pjm.com` returns a clean 401 (key required, not geo-blocked). **Revised order by measured access: PJM next (Clyde registers a free key at apiportal.pjm.com — reachable right now), then ERCOT and SPP together once a US network path exists (US-exit VPN, or back in New York — SPP needs no key at all from a US connection), then ISO-NE.** Same discipline as CAISO/NYISO throughout: separate adapter per market, price at published granularity, carbon explicitly balancing-area scoped from EIA-930, never relabelled nodal; a US overview view is now worth drawing once one more US market lands (three would then exist).

## Folder structure

```
grid-aware-scheduler/
├── HANDOFF.md          ← this file, always read first
├── adapters/           ← GB, GB regional and CAISO market boundaries
├── app/                ← five pages, JSON API and loopback server
├── core/               ← planning, estimation, routing, audit and backtest logic
├── hardware/           ← catalogue, local providers and empirical calibration
├── data/               ← generated market, audit and calibration state
├── docs/               ← algorithms, API, calibration and readiness boundary
├── tests/              ← offline unit, generated-page and real-HTTP checks
└── notebooks/          ← exploratory analysis
```

**External dependency: the National Grid Tool.** `adapters/gb_adapter.py` imports from a sibling folder, `AI Energy/National-Grid-Tool` — Clyde's own earlier GB power-market data project. It serves **two** purposes here, and they are different things:

1. **A runtime dependency.** The adapter imports its `carbon_intensity` and `elexon.prices` clients via `sys.path` at runtime (see top of `gb_adapter.py`; override with the `NATIONAL_GRID_TOOL_PATH` env var). If that folder moves or is renamed, `gb_adapter.py` breaks until the path is updated.
2. **The architectural reference this project is modelled on.** See "Architecture we're borrowing" below.

**It is deliberately NOT part of this git repo** — not vendored, not copied, not a submodule. It is a separate production tool with its own lifecycle. Do not commit it here, and do not rename or strip it down: this project imports it at runtime by that exact folder name, and removing parts of it buys this repo nothing since it was never going to be committed anyway.

**Setup on a fresh machine:** create the venv from *that* project's requirements, at the location its README specifies —
```bash
python3 -m venv ~/venvs/national-grid
~/venvs/national-grid/bin/python -m pip install -r "../National-Grid-Tool/requirements.txt"
```
then run everything in this project with `~/venvs/national-grid/bin/python`. (Note: pip currently resolves pandas 3.0.x, which breaks 3 date-parsing tests in that project's news/weather feeds. Harmless here — nothing in the carbon or price path is affected, and all of those tests pass.)

## Architecture we're borrowing from the National Grid Tool

Worth understanding before writing more adapters, because it's why `gb_adapter.py` is only ~130 lines:

- **The settlement-period grid is the universal join key.** 48 half-hourly periods per day. All ~28 fetchers in that project are forced to land on the same `settlement_date` + `settlement_period` + `start_time` index, which is why two totally unrelated APIs (Carbon Intensity and Elexon) merge on a two-column join with no dtype fight. `sources/elexon/_frames.py::to_frame()` is the enforcer — it snake_cases keys, coerces timestamps to UTC-aware, and pins `settlement_date` to a plain `date`.
- **Layering:** HTTP client (retry on 429/5xx/timeouts only, typed errors) → fetchers returning tidy frames against a declared column constant → `warehouse/store.py` → UI. Each layer only knows the one below it.
- **The warehouse is one API over two backends**, routed by table name: SQLite for settlement-keyed feeds, Parquet+DuckDB for the high-volume per-BM-unit feeds (~10GB vs ~400GB). Callers never know which answers. Writes are idempotent by delete-then-append on `settlement_date`.
- **Why this matters for us:** `GridDataPoint` is just a narrowed projection of a contract that already existed, and the settlement period is a natural scheduling decision slot. The calculation falls straight out — for a job drawing `P` kW over one period: `energy_kWh = P × 0.5`, `cost = energy_kWh × price_£MWh / 1000`, `carbon_g = energy_kWh × intensity_gCO2kWh`. A deadline-constrained flexible job is then a sliding-window argmin over a few hundred floats. No solver, no ML — which is exactly the transparent classical approach argued for in Open Questions.

## Location awareness (built 2026-08-09)

**Location turns out to be a bigger lever than time.** Time-shifting a job on the national signal saved 54% carbon. Moving it between GB regions, measured live: **North Scotland 0 gCO2/kWh (96% wind) against East Midlands 131, at the same instant.** Google's carbon-intelligent computing work shifts on both axes; this project now has the data to do the same.

- `adapters/gb_regional.py` — all 18 GB grid regions, postcode -> region lookup (accepts full postcodes and normalises to the outward code), and a 48-hour forward half-hourly carbon forecast per region.
- `adapters/weather.py` — Open-Meteo forecast for any lat/lon: temperature, solar radiation, 100 m wind speed, cloud cover. Public, keyless, **worldwide** — so unlike the GB carbon endpoints this already works for markets that have no adapter yet, and it is the feed the bespoke CAISO/ERCOT forecast work would build on.

Weather explains carbon rather than merely sitting beside it — measured across the presets in one pass:

| location | region | gCO2/kWh | wind 100m | usable wind |
|---|---|---|---|---|
| Thurso | North Scotland | 0 | 10.8 m/s | 83% |
| Edinburgh | South Scotland | 1 | 8.5 m/s | 59% |
| Manchester | North West England | 40 | 4.2 m/s | 19% |
| London | London | 90 | 3.4 m/s | 14% |

**The constraint that shapes the UI — in GB, location changes CARBON but not PRICE.** GB settles one national wholesale price, so moving a job from London to Scotland changes its emissions and nothing about its bill. Locational price variation is real but lives in nodal markets (CAISO, ERCOT) where thousands of nodes settle separately. A GB location control therefore drives carbon and weather only. **Never present a GB map as though it varied price** — that becomes true when the nodal-market adapters land, and not before.

## UI and deployment (decided 2026-08-09)

**The real system cannot be a web portal, and this is forced, not preference.** A browser cannot read hardware power draw — NVML, `powermetrics`, `system_profiler`, Metal device enumeration are all unreachable from a web page. And a scheduler that defers a job to a window six hours out has to still be running six hours later. So the real system needs a local, long-lived process with OS access.

**But "web portal vs desktop app" is a false binary.** The answer is a local process serving a browser UI — how Jupyter, Ollama and the National Grid Tool itself all work. Browser-based UI with instant iteration and no packaging, on top of a native process with unrestricted hardware access. No code signing, no notarisation, no per-platform builds.

**Not Streamlit, though.** It imposes a strong visual identity that can't be reshaped into a native-feeling interface without CSS overrides that break on version bumps. `app/dashboard.py` instead generates plain HTML/CSS/SVG we own outright — no framework, no CDN, no build step, one file that opens in any browser. The Apple-style design language is deliberate.

The layering, each knowing only the one below it:

1. **Core engine** (`core/`, `adapters/`) — importable, testable, no UI, no server. *This is the actual product.*
2. **Agent** — the long-lived local process: detects hardware, holds the job queue, wakes to launch deferred jobs. Later a `launchd`/`systemd` service. Not built yet.
3. **UI** (`app/`) — thin and replaceable.

**Two artefacts, two deployment stories, and they don't conflict:** the `demo/` simulator is a pitch/communication piece and is a static page that can ship on GitHub Pages for a shareable link; the scheduler is the real system and installs locally. Keep them separate.

**Running it — two ways, and the difference matters:**

```bash
# 1. Serve it (normal way). Rebuilds from live data on reload.
~/venvs/national-grid/bin/python -m app.serve          # → http://localhost:8765

# 2. Render one file (for sharing a snapshot, or offline).
~/venvs/national-grid/bin/python -m app.dashboard --days 3 --open   # → file://…
```

**"In the browser" is not "on the web", and the distinction is the whole architecture.** `app.dashboard` writes an HTML file that the browser opens off disk over `file://` — no server, nothing listening. `app.serve` runs a local process bound to `127.0.0.1` (loopback only — other machines on the network cannot reach it, and nothing outside can). Both are entirely local. Neither is hosted anywhere.

That is exactly what makes the rest of the plan possible: a hosted web page could never read `powermetrics` or stay alive to launch a job deferred six hours out, but a local process serving a local page does both while still giving a browser-quality UI. Only fleet mode across multiple machines ever needs real hosting.

Generated output lands in `app/build/` (gitignored — it rebuilds in seconds). The server caches for 5 minutes, since the underlying signals only move on a half-hour settlement boundary.

**Charting — custom, not a library. Decided 2026-08-09 after trying KLineCharts.**

The target is a good *consumer finance* chart (Trading212, Yahoo), which is not the same thing as a trading terminal — and the difference decided the build. A terminal gives free pan/zoom and technical indicators; an RSI of carbon intensity is meaningless. A consumer chart gives a **range selector** and a **crosshair that reads out in a fixed header slot**, and that is roughly two hundred lines of SVG.

`app/chart.py` + `core/resample.py`. No library, no CDN, ~125 KB page, full design control.

**The domain reason this could not be delegated to a charting library:** bucketing must keep the extremes. Naive downsampling takes each bucket's mean — and for a scheduler that is actively destructive, because the whole job is finding the *cheapest half-hour*. A day whose 30-minute prices ran £2.84 to £158.94 has a daily mean near £80, and both numbers that matter are gone. So every bucket keeps mean, min and max; the chart draws the mean as a line with the min-max range as a band behind it. Zooming out costs resolution and never costs the extremes. `tests/test_resample.py::test_extremes_survive_aggregation` guards this, because a mean-only chart still *looks* fine — the failure is invisible by inspection.

**Bug worth remembering: the scheduling decision must run over the horizon a job actually faces, not over whatever history the charts show.** Feeding the full 21-day series to `compare()` produced a "run immediately" baseline dated three weeks earlier — not a decision anyone could act on — and placed the chosen window off the edge of every visible chart range. The dashboard now decides over `series[-deadline_periods:]` and states the window it decided over. Charts still show full history; the decision does not.

**Chart completeness — what a grid chart actually needs.** A first version shipped with no y-axis at all: gridlines and value labels were missing entirely, so the reader could see that carbon rose but not to what. Also missing was any link back to the decision. Both now fixed:
- Y-axis with round-number ticks (snapped via a nice-number step, because "190" is a number a reader holds and "187.4" is not), in a left gutter so labels never sit on the plot.
- Highlighted spans mapped from timestamps, so the window the scheduler chose is visible on the curve and stays correct across range changes.
- Range summary: high, low, average and **spread**, the last being the headline for a scheduler — it is the size of the prize for shifting a job at all.
- A settled/forecast divider, because one unbroken line implies both halves are the same kind of claim and they are not.

**Still missing from Page 1, and it is not finished:** generation mix by fuel, region selection and the 18-region comparison, weather alongside carbon, forward forecast, percentage change over the range, and weekend/overnight shading.

**Bug worth remembering: axis format comes from the SPAN, not the BUCKET.** They are different questions. A 1-month range buckets into 2-hour bars, so the bucket is "intraday" — but labelling a month-wide axis `22:00, 20:00, 04:00` tells the reader nothing about where they are. Span decides the axis; bucket decides the hover readout. Caught only by rendering it and looking.

Ranges: 1D (30 min) · 1W (30 min) · 1M (2 h) · 3M (6 h) · 1Y (1 day) · Max (1 week). All bucketed server-side and embedded, so switching range is a repaint, not a fetch.

**KLineCharts is kept but unused** (`app/kline.py`, vendored v9.8.12, Apache-2.0) in case a power-user terminal view with indicators is ever wanted. Its lessons are recorded below.
- **Hand-written SVG** for everything KLineCharts is not for: generation-mix breakdowns, stat tiles, sparklines, fleet and routing diagrams. It is a *financial candlestick* library — a fuel-mix chart is not an OHLC series and forcing it through would be worse than drawing it directly.

**Jank already hit and fixed, so nobody hits it twice:**
- The formatter API is `chart.setCustomApi({formatDate})`, **not** `setFormatter`. Calling the wrong name throws, and since it sits before `applyNewData` the chart draws styled axes and *no data at all* — it looks like a data problem and is actually an exception. Check the vendored bundle's exported names before guessing an API.
- The default tooltip lists open/high/low/close. For a line series that is the same number four times. Replaced with a custom tooltip showing time and value only.
- Axis label weight cannot be set for dates independently of times — the library styles the whole bottom axis as one group.
- Library defaults print a bare `HH:mm` with no date, and an ambiguous `MM-DD`. Custom `Intl.DateTimeFormat` formatters spell the month and keep the year, and drop clock times once bars are daily or coarser.
- Timezone: `chart.setTimezone()` rebuilds the internal formatter and repaints, so formatters read `dtf.resolvedOptions().timeZone` rather than tracking toggle state — one source of truth.

**Not yet verified:** the click-to-read tooltip renders correctly. It could not be confirmed in a headless screenshot because KLineCharts handles pointer events on its own canvas overlay and ignores synthetic ones. Needs checking with a real mouse on the served page.

**Charts:** the palette is Apple's system colours snapped to steps that pass a colour-accessibility validator in *both* light and dark — lightness band, chroma floor, colour-blind separation, normal-vision separation, and contrast against the surface. The dark series colours are deliberately **darker** than Apple's own dark system colours, which sit above the band a chart needs. Don't hand-edit `CARBON_*` / `PRICE_*` in `app/dashboard.py` without re-validating. Carbon and price get separate charts on purpose — never a dual y-axis.

## A note on the `demo/` simulator

`demo/task_difficulty_routing_simulator.html` is a **throwaway communication prototype**, not a spec. A slider for operation count, a dropdown for task type, four fixed hardware tiers. It exists to make the task-difficulty-routing idea visible before the real thing exists.

Do not let its simplicity anchor scope. The real system replaces every part of it: real task classification instead of a dropdown, auto-detected and benchmarked hardware instead of four labelled boxes, real multi-GPU topology and interconnect awareness, and grid-signal timing feeding the routing decision — all at once. Keep the prototype as a reference for the interaction idea; build nothing from its structure.

## Hardware detection (replaces static GPU profile CSV)

Decided 2026-08-09: instead of hand-curating a CSV of published GPU specs, the profiling module should auto-detect whatever compute units are actually present on the machine it's running on, then either match against known specs or empirically benchmark on the fly. This is more credible than trusting published TDP numbers (which are idealized) and works on hardware nobody's pre-catalogued.

Two separate steps, don't conflate them:
- **Detection** (easy): enumerate what's present. NVIDIA/cloud → `nvidia-smi` / NVML (`pynvml`) lists every GPU + live stats. Apple Silicon → `system_profiler`, `sysctl` (`hw.perflevel0/1` for P-core/E-core counts), Metal's `MTLDevice` API for GPU.
- **Profiling** (harder, more valuable): measure real efficiency under load rather than trust a spec sheet. NVML gives live power draw + utilization per NVIDIA GPU. macOS `powermetrics` (needs sudo) gives real per-component power draw — CPU, GPU, ANE — while a task runs. Fallback to known-spec lookup only when live measurement isn't available.

## The portal — two pages (built 2026-08-09)

Served together by `app/serve.py` at `http://localhost:8765`:

- **`/` — Grid.** Live carbon and price, the scheduling decision, range-selector charts. Not finished: generation mix, region selection, weather, forward forecast.
- **`/simulator` — Model simulator** (`app/simulator.py`). Pick a model, hardware, count and task; get runtime, power, energy, memory feasibility, and — via the live grid signal — cost and carbon. 840 configurations precomputed server-side and embedded, so every control is instant with no round trip.

The simulator carries three views: the selected configuration, **every device ranked by energy** for the same job, and **the same device at every fleet size**. The second and third are where the argument lives, because they make two results visible at a glance:

- Adding hardware does not save energy. 1x and 64x H100 both use ~22-23 kWh for the same job; only runtime moves (32.1 h to 31 min).
- Choosing hardware does. On an 8B training run the spread across the catalogue is 16 kWh (H100 PCIe) to 370 kWh (M2) — over 20x, for identical work.

Provenance is on screen as a badge, not buried: SPEC for datasheet-backed rows, ESTIMATED for Apple, where no vendor figures exist.

**On-site renewables (`core/renewables.py`, added 2026-08-09).** Given a location and installed solar/wind capacity, computes how much of a job's load on-site generation actually serves, period by period, and how much must still be imported.

**Built rather than called out to Renewables.ninja, deliberately.** Renewables.ninja is better modelled — MERRA-2/SARAH reanalysis, real turbine curves, bias correction validated against metered output — but it needs a token, rate-limits to tens of requests an hour, and answers in seconds. A simulator that recomputes on a control change cannot live on that. The physics is a few lines and the inputs (irradiance, 100 m wind) are already fetched from Open-Meteo for the weather panel, so this runs locally in microseconds. What is given up, and it must be labelled ESTIMATED because of it: no bias correction, a generic turbine curve, no shading/soiling/downtime, and irradiance treated as plane-of-array. **Expect the shape right and the level optimistic.** Validating against Renewables.ninja on a handful of sites is the obvious next step — same "derive, then check against a known-good source" method as the hardware profiling.

**The number that matters is hourly matching, not annual.** Measured live at Thurso with 500 kW of each: the site generates **191% of what it needs** across the period yet covers only **90% of it**, still importing 930 kWh — because the generation arrived when the load did not. Netting annual totals would call that fully renewable. This is what 24/7 carbon-free accounting means and why the page reports both figures side by side with the gap called out.

**Simulator rewritten to compute client-side (2026-08-09).** The first version precomputed every (model, task, device, count) combination server-side and embedded the lot. That capped the catalogue at whatever the payload could carry — five models — and made a custom model impossible, because you cannot precompute a number nobody has typed. Now the device and model specs ship and the arithmetic runs on each control change. The page went from 122 KB to **32 KB** while going from 840 fixed combinations to 35 models x 12 devices x 11 fleet sizes x 6 precisions, plus any custom model. Precomputing was the expensive way to do less.

`core/models.py` holds the catalogue — Llama, Mistral, Qwen, Gemma, Phi, DeepSeek, Command R, Falcon, Yi, GPT-OSS. **MoE models carry two parameter counts and conflating them is the classic error:** compute scales with *active* parameters, memory with *total*. DeepSeek-V3 is 671B total and ~37B active — the compute of a 37B model and the memory of a 671B one. Both true; a simulator tracking one will confidently mislead.

**Page 1 now shows regions.** All 18 GB grid regions with live carbon and generation mix. The adapters had existed for hours while the page still showed national-only data, which was the fair criticism. Measured live: North Scotland **0 gCO2/kWh on 100% wind** against South West England at **358 on 91% gas** — same country, same instant. Moving a job is a bigger lever than delaying one, and the page now says so.

**Still to build: page 3, the planner.** Given a job, a heterogeneous fleet ("3 NVIDIA, 2 Intel Arc, 1 AMD" or 25 H100s) and a deadline, decide how to split the work across devices and when to run each part, optimising cost and carbon together. `hardware/base.Fleet` already models heterogeneous groups and `core/workload` already splits work by achievable throughput; what is missing is placing those splits in *time* against the grid signal.

## Local cache — the thing that made date ranges honest (built 2026-08-09)

`core/store.py` (SQLite) + `core/backfill.py` + `core/feed.py`.

**The bug it fixed was a lie in the UI.** Live fetching costs ~0.3s per day of history, so the app fetched three weeks — then offered 1M, 3M, 1Y and Max range buttons that all rendered *the same three weeks*. Four controls, one view, no indication anything was wrong.

Now: **395 days cached, 18,864 rows, 3.2 MB, and a full year loads in 23 ms.** Backfill takes ~2 minutes once and is safe to re-run or interrupt (idempotent per settlement date). Only settled days are cached — today and tomorrow are forecasts that change, so those always go live.

```bash
python -m core.backfill --days 400     # one-time, ~2 min
python -m core.backfill --stats
```

**Everything above the data layer now calls `core.feed.load()`** and never touches an adapter directly. `feed.extent()` reports what data actually exists, so the range selector can stop offering ranges it cannot fill.

**A year of data changes the story.** Over 2025-07 → 2026-08: price ran **£0.09 to £560.81 — a 6,231× spread** — and carbon 17 to 319 gCO2/kWh. The three-week window had been hiding almost all of the opportunity this project exists to exploit.

## Allocation path map (built 2026-08-09)

`app/flow.py` (server-side renderer) and a client-side version inside `app/simulator.py`, plus a fleet builder for mixed-vendor estates.

Three stages — **JOB → DEVICES → WINDOW** — drawn as ribbons whose width is each group's share of the work, because the proportions *are* the decision. Verified with 8x H100 + 4x Arc A770 + 2x MI300X + 1x M2 Ultra: the H100s take 79.1% and the MI300X 19.9%, so every group finishes together. Split that fleet evenly instead and the slowest sets the finish time while the fastest idle at part load, burning power for nothing — which is precisely what a uniform-fleet demo cannot show.

## Chart: now a terminal, not a picture

Area / line / **candles** / bars, wheel zoom centred on cursor, drag-pan, shift-drag box select, keyboard control, MA/EMA, UTC↔Local, CSV, full OHLC crosshair. **Interval and range are separate controls** — conflating them is why candles rendered as flat dashes (each bucket held one reading, so O=H=L=C). Bucketing is client-side and zoom is stored as a *time window*, so changing interval keeps you on the same stretch of time.

Two deliberate departures from a financial terminal: a **spread sub-pane** instead of volume (the high-low range within an interval is the size of the prize for shifting a job; volume is meaningless here), and **percentile bands** instead of oscillators ("is now in the cheapest decile" is the real question; an RSI of carbon intensity is not).

## Three ways I have now blanked the charts — read before editing generated JS

Each was invisible to the check I had at the time:

1. **A raw newline inside a JS string literal.** Syntax error, kills the whole script block. Grepping found the code present and correct-looking. **Fix: `tests/test_pages.py` parses every generated script with `node --check`.**
2. **An unsubstituted `__HSUB__` placeholder.** A valid JS identifier, so `node --check` passed it; it only failed in a browser. **Fix: substitution raises at build time, and a test asserts no `__TOKEN__` survives.**
3. **A `str.replace()` whose anchor did not match**, so an entire JS block was silently dropped — the markup rendered, the behaviour did not. **Fix: assert the anchor exists before replacing.**

**And the rule underneath all three: render it and look at it.** Every one of these passed its automated check. Screenshot the page after any change to generated JS.

Note both page modules build HTML with **f-strings**, so braces in injected JS must be doubled (`{{`/`}}`). `app/chart.py` avoids this entirely by using a plain string with `__TOKEN__` placeholders — the better pattern, and worth porting `simulator.py` to.

## Analytics panels — built for the actual buyer (2026-08-09)

Target user is now explicit: **a data-centre operator with a carbon-neutrality target.** `core/analytics.py` + `app/panels.py` answer the questions that buyer has, measured over the whole cached year rather than one week.

**Findings that should shape the pitch, all measured on 392 days of real GB data:**

- **Flexibility is quantified.** A 4-hour job saves **21.9% median on cost at a 24-hour deadline**, and **75% at a week**. Carbon saves **28.5% at 24 hours**. Every start in the history, not one anecdote.
- **Cheap is not clean.** Price/carbon correlation is only **r = 0.54**, and the cheapest decile is also the cleanest decile just **59% of the time**. An operator optimising purely on price misses the carbon target 41% of the time — which is the single strongest argument that the objective has to be *stated*, not inferred.
- **Carbon is less volatile than price**, so carbon savings from time-shifting are smaller and steadier. Location, not timing, is the bigger carbon lever (North Scotland 0 vs South Wales 358 gCO2/kWh, same instant).

Panels: savings-vs-deadline (cost and carbon), time-of-day profile (both), duration curve (both), price-carbon scatter. Deliberately non-interactive — a duration curve is a conclusion over a year, and giving every panel a toolbar would bury the two charts where interaction matters.

## Design direction changed (2026-08-09)

The Apple design language was **dropped as the primary goal**. It is optimised for calm and touch; this is a data terminal for an operator, and the two pull in opposite directions — large type and generous padding mean fewer things on screen, which is the opposite of powerful. Density is now the goal. The analytics grid is the first piece built that way; the rest of both pages still needs converting from the vertical card stack.

## Panels expand on click (2026-08-09)

Click any analytics panel to fill the screen; Escape or a second click closes it. The SVGs are viewBox-based so scaling is free — the panel becomes a fixed overlay and the same markup fills the viewport. `EXPAND_JS` in `app/panels.py`.

**The two main charts do not expand yet** — they have their own toolbar and zoom, so the interaction needs designing rather than copying.

## Still outstanding — ordered by what a buyer would notice first (2026-08-09)

**Credibility (changes whether the numbers can be trusted):**
1. **KV cache is modelled but not wired into the simulator UI.** `core/models.py` has architectures, GQA and `kv_cache_gb()`; the page still uses a flat 1.25x multiplier for inference memory. Wrong by orders of magnitude at long context — a 70B at 128k across 32 sequences needs ~1.3 TB of KV cache against 141 GB of weights. **Needs context-length and batch-size controls.**
2. **No PUE / cooling overhead.** Datacentre energy is not chip energy. Every kWh, GBP and kgCO2 figure on the simulator is optimistic by roughly 1.2-1.5x. A single multiplier with a control would fix it, and for the target buyer this is a *headline* number, not a detail.
3. **No sharding model** (FSDP/ZeRO). Multi-GPU training memory is therefore wrong — it assumes weights replicate.
4. **Scaling model covers communication only.** No pipeline bubbles, stragglers or failure recovery, so it reports ~99% at 32 devices where reality is 70-85%. Documented in the code, still optimistic.

**Depth (changes whether it feels like a product):**
5. **Density.** The analytics grid is dense; everything else is still a vertical stack of large cards. Convert both pages to a multi-panel layout. The Apple design language was dropped as the primary goal for this reason.
6. **No URL state** — a configuration cannot be shared, bookmarked or returned to. For a tool an operator would show a colleague, this is close to essential.
7. **57 devices in a plain `<select>`** with no search, grouping or filter.
8. **Main charts do not expand**; panels now do.
9. **No cross-filtering** — selecting a region or a window does not filter anything else.
10. **Simulator has no analytics panels** — sensitivity to price, carbon, utilisation and fleet size all belong there.

**Missing pieces of the stated vision:**
11. **Page 3, the planner** — job + heterogeneous fleet + deadline, deciding the split *and* the timing together. The path map draws the split; nothing yet chooses the timing per group.
12. **US markets.** CAISO OASIS was proven reachable keyless (24h of SP15 day-ahead LMP, $28.86-$68.88). Nodal pricing means location changes *price* there, not just carbon — the strongest demonstration of the whole idea, and still unbuilt.
13. **Auto hardware profiling** with the validation method already designed above.
14. **Custom model auto-analysis** — the user enters a model and the tool profiles it.



- **KV cache is modelled in `core/models.py` but not wired into the simulator UI.** Inference memory there is still a flat 1.25x multiplier, which is wrong by orders of magnitude at long context: a 70B at 128k across 32 sequences needs ~1.3 TB of KV cache against 141 GB of weights, and grouped-query attention swings it 8x. **Needs context-length and batch-size controls.**
- **PUE / cooling overhead is absent.** Datacentre energy is not chip energy; every figure is optimistic by roughly 1.2-1.5x.
- **No sharding model** (FSDP/ZeRO), so multi-GPU training memory is wrong.
- **Scaling model covers communication only** — no pipeline bubbles or stragglers, so ~99% at 32 devices where reality is 70-85%.
- **No URL state**, so a configuration cannot be shared or returned to.
- **57 devices sit in a plain `<select>`** with no search or filter.
- **Density**: the pages still read as an iOS app rather than a data terminal — large type, generous padding, one column. Needs a denser multi-panel layout.
- **Page 3, the planner**, still does not exist.

## Auto hardware analysis, and how to prove it works (planned 2026-08-09)

**The goal: nobody types in specs.** A simulation runs on the device, and from how it behaves the system derives what that hardware actually is — throughput, memory bandwidth, power curve, and how it scales. Manual spec entry is a stopgap.

**The validation method is the valuable part, and it is genuinely falsifiable.** Run the profiler on hardware whose real specifications are already published, then compare what the algorithm derived against those known figures. If the derived numbers track the datasheet across several different devices, the algorithm can be trusted on hardware nobody has catalogued. If they diverge, that is a measurable error to fix, not a matter of opinion.

That matters more than it might sound. Every simulator in this space asks to be taken on trust. This one can state an error bar: *"derived within X% of published spec across N devices."* That is a claim with a number attached, and it is the kind of thing that survives a technical audience.

Sequence:
1. Micro-benchmarks -> derive achievable FLOPS, memory bandwidth, sustained power. Start with what is measurable here: an M2 with 8 GB, using `powermetrics` for real watts.
2. Compare derived against `hardware/catalog.py` published figures. Record the delta per device.
3. Widen to whatever hardware can be borrowed or rented — a single cloud GPU-hour is enough for one calibration point.
4. Only once the deltas are known and small: trust the profiler on uncatalogued hardware, and report the error bar alongside every derived figure.
5. Then replace the analytical scaling model with **measured** scaling curves, which is the weakest part of the current simulator (see below).

**Apple Silicon inverts the validation method, and is the stronger case for it.** Apple publishes core counts and sometimes memory bandwidth — and no GPU TFLOPS, no per-component TDP, nothing else the simulator needs. So `hardware/catalog.py`'s Apple rows are marked ESTIMATED, not SPEC: they are community benchmarks, not datasheet values. There is no ground truth to check a profiler against.

But the architecture is uniform. An M2, an M3 Max and an M2 Ultra are the same GPU core design in different quantities with different memory bandwidth. That makes a different and arguably better test available:

> Profile one Apple chip. Derive per-GPU-core throughput and per-core power. **Predict** a different Apple chip from its core count and bandwidth alone. Then measure that chip and see whether the prediction held.

Matching a datasheet only shows you can reproduce a published number. Predicting an unmeasured device from constants derived on another one is a real out-of-sample test — the thing actually claimed when the profiler is pointed at uncatalogued hardware. Apple is the best platform to run it on precisely because the architecture is consistent and the specs are absent.

The first calibration point is available now: the M2 in use here, 8 GB, `powermetrics` for real watts. The 8 GB limit caps it to small models, which is fine — deriving per-core constants does not need a large model.

**Known weakness this would fix.** `core/workload.scaling_efficiency` models communication cost only — gradient all-reduce over the interconnect. Real large-run inefficiency also comes from pipeline bubbles, stragglers, load imbalance and failure recovery, none of which are modelled. So it reports ~99% efficiency at 32 devices where a real run might see 70-85%. Sound for comparing configurations; not a scaling prediction, and it must not be quoted as one.

**Findings already worth keeping from the first simulator run** (70B training, 1B tokens):
- **More devices does not save energy — it saves time.** 1x H100 and 32x H100 both consume ~197 kWh; only the runtime changes (281 h vs 8.9 h). Under near-perfect scaling, total energy is invariant.
- **Hardware choice does save energy.** 8x A100 needs 333 kWh for the same job that 8x H100 does in 197 kWh — a 40% difference from device selection alone.
- Together these say something useful about the whole project: the savings come from *which* hardware, *where*, and *when* — not from *how much*.

## Possible platform target: Apple Silicon companion demo

Not yet committed — logged here so it isn't lost. Clyde's dream outcome is a warm-intro conversation with someone senior at Apple, same "build something credible, start a conversation" strategy as the core project (see [[project-grid-aware-scheduler]] memory).

The core architecture (fleet of interchangeable GPUs + wholesale grid market price) doesn't port literally to a single Mac — one SoC, no GPU fleet to route between, no wholesale electricity market relevant to one device's draw. But there's a legitimate structural reframe, not a stretch: Apple Silicon has heterogeneous compute units on one chip (CPU P-cores/E-cores, GPU, Neural Engine) with different power/throughput curves per task — same shape as "different GPU types" at the chip scale instead of the rack scale. Pair that with a real consumer time-of-use tariff feed (e.g. UK Octopus Agile API, the standard one people build these demos against) instead of wholesale price, and the personal-device version becomes: delay non-urgent on-device ML jobs to cheap tariff windows, and route each task to whichever compute unit (CPU/GPU/ANE) is most power-efficient for it.

Framing discipline (same rule as the rest of this project): Core ML already does automatic compute-unit dispatch. Any write-up must be honest that this adds cost/time-of-use awareness on top of existing dispatch, not that it invents compute-unit selection from scratch.

Plan if pursued: keep the cloud/DC version as the primary credibility piece (real prior art, real stakes, matches the current AI-datacenter-energy story). Build the Apple Silicon variant as a second, smaller companion demo aimed specifically at an Apple conversation, since it speaks their language (performance-per-watt, on-device ML) more directly.

## Scope decisions from 2026-08-09 planning session

No fixed deadline for showing this to anyone — pace is set by getting things genuinely right, not by a target date. Building on existing published research/reasoning (Compute Gardener, Eco-Orchestrator, Zeus/Perseus, academic carbon-forecast methodology) and improving on it with better execution is legitimate and should be cited, not treated as something to avoid referencing.

**Forecasting — researched, decision made:** checked whether GB's National Grid ESO Carbon Intensity forecast is already "genuinely direct from source" (Clyde's own bar for whether a bespoke model is worth building). Answer: yes, for GB specifically. It's built with University of Oxford Dept of Computer Science + Met Office weather data + WWF/EDF Europe, uses ML model ensembling into an optimised meta-model, a full reduced GB network power-flow model (active/reactive flows, system losses, impedance), regional granularity, nowcasting updates every 30 min, 96+ hours ahead. This is not a naive model — trying to out-forecast it specifically for GB is not a good use of effort.

However: CAISO and ERCOT do **not** have an equivalent free, transparent, operator-published forecast. Public access there runs through third parties (WattTime MOER, Electricity Maps — often paywalled/subscription, EIA raw generation-mix data) rather than a documented in-house model like GB's. That's real, verified room for a bespoke forecast to add value — not by beating GB, but by providing something methodologically transparent that doesn't otherwise exist for free in those markets.

**Decision:** build an own forecasting model, grounded in published research methodology (not invented from scratch), pulling from multiple sources (not just one grid operator's feed). Validate it first against GB's published forecast as the best available ground truth (since GB's is known-good), then apply the validated methodology to CAISO/ERCOT where no free equivalent exists. Do not claim it beats GB's forecast — frame it as consistent, transparent methodology extended to markets that lack GB's infrastructure.

**Hardware scope — decision:** include multi-GPU topology/interconnect-aware clustering (how GPUs work together in groups, not just individual device benchmarking) in scope now, not deferred to later. This is harder than single-device profiling but is core to the vision of auto-arranging clusters, not an add-on.

**Primary differentiator — decision:** task-difficulty-based routing (classifying a task's difficulty — e.g. complex math vs. verbal reasoning — and routing it to the right hardware/cluster configuration, combined with grid-aware timing) is the main focus. This remains the one piece not found anywhere in existing prior art (Compute Gardener, Eco-Orchestrator are task/content-agnostic — they schedule by resource request, not inferred task difficulty).

**Correction, 2026-08-10:** semantic difficulty must not route directly to hardware for a fixed model. Given the same model and token shape, a conceptually harder prompt does not have a defensible different FLOP or memory requirement. The correct chain is workload class -> measured quality floor -> eligible model deployments -> hardware/location/time optimisation. `core/routing.py` implements this quality-constrained boundary and requires one named, versioned evaluation suite. The earlier direct task-type-to-hardware prototype remains a communication artefact, not an algorithm specification.

Companion demo planned: an interactive visual simulator (slider for number of operations + task type, animated branching-path visualization showing which hardware/route a task would take) to communicate the concept before the real backend and hardware detection exist. Lives separately from the core scheduler code (e.g. `notebooks/` or a new `demo/` folder) — a prototype/pitch tool, not production logic.

**Relation to prior art — decision:** study Compute Gardener and Eco-Orchestrator's source/papers for reasoning and design choices, but build an independent implementation. No direct code reuse, no plan to contribute upstream to Compute Gardener at this time.

## Open questions / decisions not yet made

- Whether forecast-carbon + day-ahead-price is the right pairing long-term, vs. also pulling settlement/actuals for backtesting scored performance — leaning yes on the latter, not built yet.
- ~~Whether averaging Market Index price across data providers (APXMIDP, N2EXMIDP, ...) is good enough, or whether one provider should be preferred~~ **RESOLVED 2026-08-09 by looking at real numbers: do not average.** There are only two providers and `N2EXMIDP` reports structural zeros (100% zero across Aug/Jun/Feb 2026, 99.6% in Nov 2025), so averaging halves every price and, in the 99.6% case, distorts the curve's shape rather than just its level. Prefer `APXMIDP`, or filter non-positive prices first. Not yet patched into `gb_adapter.py` — to be built correctly in the real implementation. See Current State.
- Exact GPU types to profile — not yet chosen.
- Whether "urgent vs flexible" job classification is user-specified per job, or inferred somehow — not yet decided.
- Scheduling decision logic: leaning towards a plain rule-based/greedy or linear-programming allocator rather than an ML model — this is a constrained optimisation problem with known structure, and a transparent classical algorithm is more credible to a technical audience than an opaque model deciding GPU allocation, especially for a project whose whole framing is honest applied synthesis rather than novelty. ML (if used at all) would sit narrowly in forecasting future carbon/price, not in the decision itself. Not yet built either way.

## Session Log

*(Append-only — never edit or delete a past entry, even if it turns out to be wrong. If something changes, add a new entry that says so. This is what makes "catch up" reliable across tools.)*

### 2026-08-06 — session 1
- Created initial folder scaffold: `adapters/`, `scheduler/`, `data/`, `docs/`, `notebooks/`.
- Wrote `HANDOFF.md`, `README.md`, `adapters/base_adapter.py` (interface), `adapters/gb_adapter.py` (stub, raises `NotImplementedError`).
- Added this "Catch up" protocol and Session Log to make handoff between sessions and tools reliable.
- No real data pulled yet, no scheduler logic written yet.

### 2026-08-06 — session 2, same day
- Brought in Clyde's own earlier "National Grid Tool" project (a Streamlit app: 28 fetchers, SQLite+Parquet warehouse) and saved a full copy to `AI Energy/National-Grid-Tool`, sibling to this project, so any later session can read it for reference.
- Rewrote `adapters/gb_adapter.py` from a stub to a real implementation that imports that project's `carbon_intensity` and `elexon.prices` clients directly (both public/keyless APIs) and merges them into the common `GridDataPoint` format on `settlement_date` + `settlement_period`.
- Verified the join/merge logic offline against the National Grid Tool's own real test fixtures — mechanically correct.
- Could NOT verify a live network call from that sandbox: its proxy returned 403 on both `api.carbonintensity.org.uk` and `data.elexon.co.uk`. This needs to be run for real on Clyde's own machine before trusting it fully — flagged as the first thing to do next session.
- Also discussed and recorded a judgment call: scheduling decision logic should be a plain rule-based/LP algorithm, not ML — see "Open questions" below for the reasoning.

### 2026-08-09 — session 3
- Re-verified folder state: no changes since the 2026-08-06 sessions — same 4 files (`HANDOFF.md`, `README.md`, `adapters/base_adapter.py`, `adapters/gb_adapter.py`), `data/`, `docs/`, `notebooks/`, `scheduler/` all still empty. Nothing was manually edited outside these sessions.
- Discussed and logged a real architecture improvement: replace the planned static `gpu_profiles.csv` (hand-curated published specs) with a hardware detection + profiling module that auto-detects whatever compute units are actually present and empirically measures their efficiency (NVML for NVIDIA, `powermetrics`/`system_profiler` for Apple Silicon) rather than trusting spec sheets. See "Hardware detection" section above. This replaces step 4 in Next Steps.
- Discussed, and logged as NOT YET COMMITTED, a possible Apple Silicon companion demo aimed at a warm-intro conversation with someone at Apple — see "Possible platform target: Apple Silicon companion demo" section above for the honest technical mapping and the framing discipline required.
- Clyde asked directly: "has anyone done the whole project already." Researched via web search. Found this has substantially been done: **Compute Gardener** (github.com/elevated-systems/compute-gardener-scheduler) is an active open-source Kubernetes scheduler combining carbon/price-aware temporal shifting with hardware power-profile-aware placement, including GPU workload classification — essentially both of this project's pillars, already shipped. **Eco-Orchestrator/CARL** is an academic RL-based system doing the same combination, tested on 64 A100s, 34.7% carbon reduction reported. Corrected the Prior Art table and retracted the "nobody has done both together" gap claim — it was wrong and must not be repeated in any pitch. Added a "Where the real gap might still be" section: Apple Silicon/on-device is the one angle nothing found so far covers. Recommended reading Compute Gardener's actual source before writing more scheduler code, to avoid unknowingly rebuilding it.
- Went deeper on comparing mechanics, not just concepts: verified from the actual KubeFM transcript that Compute Gardener's "price-aware" scheduling is a fixed time-of-use schedule (residential/commercial rate structure), not real wholesale day-ahead market price — a real, meaningful difference from this project's Elexon day-ahead Market Index Price signal. Also confirmed Compute Gardener's GPU power profiling already uses live NVIDIA DCGM measurement, not static specs, which undercuts (not confirms) the novelty of this project's planned hardware auto-benchmarking — logged honestly rather than oversold.
- Clyde laid out an expanded, more ambitious vision: (1) a bespoke weather+multi-source-driven electricity generation/carbon forecast model grounded in research papers, (2) multi-GPU topology/interconnect-aware clustering (not just per-device benchmarking), (3) task-difficulty-based routing (classify task difficulty, e.g. complex math vs. verbal reasoning, route to best-suited hardware/cluster) combined with grid-timing, (4) deadline-guaranteed scheduling ("after 5 hours, x is done"). Researched and logged decisions — see "Scope decisions from 2026-08-09 planning session" above for full detail on each. Key finding: GB's National Grid ESO forecast is genuinely sophisticated (Oxford CS + Met Office + full network power-flow model) and not worth trying to beat directly; CAISO/ERCOT lack an equivalent free transparent operator forecast, which is where a bespoke model has real room to add value. Task-difficulty-based routing confirmed as the primary differentiator (not found in any prior art). Multi-GPU topology work pulled into current scope rather than deferred. Decided to study Compute Gardener/Eco-Orchestrator independently rather than reuse or contribute code.
- Building an interactive visual simulator/prototype for task-difficulty-based routing per Clyde's request (slider for number of operations + task type, animated path visualization) — see file in `demo/` if created this session.

### 2026-08-09 — session 4, later the same day
- Clyde moved `National-Grid-Tool` into `AI Energy/` (it had been sitting at `Desktop/National-Grid-Tool`), which is exactly where `gb_adapter.py`'s default path expects it. The import path issue that would have blocked step 2 is therefore gone.
- **Set up the environment on Clyde's Mac**: venv at `~/venvs/national-grid` (Python 3.12.9), installed from the National Grid Tool's `requirements.txt`, at the location that project's own README specifies (outside the project folder). Ran its test suite: 512 passed, 3 failed — all three are RSS/date-parsing in the news and Met Office feeds, caused by pip resolving pandas 3.0.x. Nothing in the carbon or price path is affected; every Elexon and Carbon Intensity test passes.
- **Ran `GBAdapter().get_data()` live for real** — the thing flagged as "first thing to do next session". It works: 2026-08-01 → 2026-08-07 returned 289 `GridDataPoint`s, 0 missing prices, carbon 31–186 gCO2/kWh. Steps 2 and 3 in Next Steps are now done. The offline fixture verification held up against live data.
- **Benchmarked it**: ~0.3s per day fetched, 92% of which is the Carbon Intensity endpoint (one call per day, vs prices chunked 5 days per call). ~2 min for a full year. Added step 3b — route through the National Grid Tool's warehouse instead of re-fetching live every time.
- **Found a real bug in the price path** by inspecting live provider-level data: `N2EXMIDP` publishes structural zeros, so averaging across MID providers halves every price (measured £57.44 vs true £115.00), and in Nov 2025 does so non-uniformly, distorting curve shape not just level. Clyde's call: **do not patch the current adapter** — carry it forward and build it correctly in the real implementation. Logged in Current State and resolves Open Question #2.
- **Initialised this folder as a git repository** with a `.gitignore`, and recorded in "External dependency" that the National Grid Tool stays out of it — separate production tool, imported at runtime, explicitly not to be renamed or stripped.
- Added an "Architecture we're borrowing" section documenting how the National Grid Tool actually works (settlement-period grid as universal join key, the layering, the two-backend warehouse) and how the cost/carbon calculation falls out of it.

### 2026-08-09 — session 5, same day
- **Caught up and found the doc was contradicting itself.** Current State said the adapter was "VERIFIED LIVE" in one bullet and "NOT yet verified" three bullets later, which led this session's opening instructions to ask for live verification that had already been done. Fixed, and the stale line left struck through rather than deleted so the failure mode is on the record. Lesson worth keeping: when marking something done, delete or strike the line that said it wasn't.
- **Built the first working vertical slice — real data end to end into a real decision.**
  - `adapters/gb.py`: corrected GB adapter. Price now comes from `APXMIDP` alone with non-positive values dropped, never a cross-provider mean. Verified: mean £111.91 over a real 2-day window, against the £57-ish the old averaging path produced. `gb_adapter.py` left untouched and superseded.
  - `core/grid.py`: market-agnostic accounting and algorithms — `run_immediately` (baseline), `cheapest_window`, `cleanest_window`, `compare`. Sliding-window minimum, no solver, no ML, per the reasoning already recorded in Open Questions.
  - `tests/test_grid.py`: 15 offline tests, all passing, zero network. Includes the rule that a window with missing price data is never selected — an unpriced window is not a cheap one.
- **Measured a real result:** 6.5 kW job, 4 h, 24 h deadline, GB 2026-08-05→08 → £3.21 to £0.21 and 2.10 to 0.97 kgCO₂. **93.6% cost, 54.0% carbon, for an 11.5 h delay.** Steps 5-7 substantially complete.
- **Found that the two objectives genuinely conflict**: on real data the carbon-optimal window sometimes costs *more* than running immediately. Logged in Current State — the scheduler must be told its objective, it cannot infer it.
- **Decided the deployment architecture** — see "UI and deployment" above. Short version: the real system cannot be a web portal because browsers cannot read hardware power draw, but a local process serving a browser UI gives both halves. Rejected Streamlit for visual control; the dashboard is hand-written HTML/CSS/SVG.
- **Built `app/dashboard.py`**, an Apple-styled dashboard rendered from live market data. Palette validated for colour-blind separation and contrast in both light and dark. Rendered and inspected both modes, which caught three defects that unit tests could not: axis ticks labelling padded bounds rather than real data (a price series with a £2.84 floor was showing "-16"), a stray hover dot parked at the origin because SVG ignores the HTML `hidden` attribute, and a "the objectives disagree" panel firing on a £0.00 trade-off. All fixed. Worth repeating the habit: render the thing and look at it.
- **Added `app/serve.py`** — a local server on `http://localhost:8765`, bound to loopback only, that rebuilds the page from live data on reload (5-minute cache; first load 1.3s, cached load 0.9ms). This is the normal way to run it; `app.dashboard` remains for rendering a single shareable file. Clarified in the doc that "in the browser" is not "on the web" — both paths are entirely local, and that is precisely what lets the real system read hardware power draw later.
- **Recorded that `demo/` is a prototype, not a spec** — see the section above, added so this doesn't have to be re-explained in every future session.

### 2026-08-09 — session 6, checkpoint 1
- Analysed the full handoff and audited the running product. The core GB data path and hardware catalogue were substantive, but the product was still two disconnected pages, had no placement planner, no US market, coarse location controls, and several optimistic workload assumptions.
- Added `core/planner.py`: exact enumeration across hardware/location/start candidates, hard memory and deadline feasibility, fractional final-period accounting, PUE, explicit cost/carbon/delay weights and a Pareto frontier. Added offline tests for joint selection, deadline/memory rejection, PUE and missing-signal behaviour.
- Added `adapters/caiso.py`: official CAISO OASIS day-ahead LMP at six selectable locations or a validated custom PNode, joined to EIA-930 fuel mix. Carbon is an explicit balancing-area production estimate using published operational factors; it excludes imports and is not labelled nodal. A live SP15 replay returned 48 half-hours with complete price and carbon data.
- Added shared `app/markets.py` semantics for GB national/regional and CAISO nodal views. The planner uses a recent complete replay because the current public feeds do not supply a complete forward price curve across both markets.
- Created `app/planner.py` with full-catalogue hardware search, exact start-window search, objective controls, hard constraints, decision trace, ranked alternatives, Pareto chart, location controls and shareable URL state. It renders and its generated JavaScript passes a parser check; server integration is still in progress.
- Corrected simulator credibility gaps: facility PUE, measured system efficiency, FSDP/ZeRO-3 versus replicated training memory, architecture/context/batch-based KV cache, grouped hardware and URL state.
- Reworked compact chart expansion into an accessible full-screen inspection view with keyboard and mobile support.
- Full project test suite at this checkpoint: 43 passing in the configured project environment.

### 2026-08-09 — session 6, checkpoint 2
- Integrated the planner at `/planner` and made `app/serve.py` parse market/location URL state. The cache is now keyed by market and location, so switching from GB national to London or SP15 cannot return the previous location's page.
- Refactored the grid dashboard for market-aware currency, source labels and carbon scope. It uses the latest interval rather than incorrectly displaying the oldest point as "right now", handles negative-price spreads without a divide-by-zero ratio, selects the latest contiguous fully scored decision horizon, and links Grid, Simulator and Planner in both directions.
- Added dashboard market controls for GB and CAISO, fourteen GB carbon regions, six CAISO hub/load-area choices, and a custom CAISO PNode entry. GB location changes regional carbon but retains national pricing; CAISO location changes node-level price while carbon remains a balancing-area estimate.
- Expanded page-level coverage to 47 passing tests, including generated planner JavaScript parsing, cross-page navigation, compact-panel keyboard/full-screen behaviour, CAISO currency and carbon labelling, and simulator PUE/KV-cache controls.
- Exercised six actual local HTTP views: GB national dashboard, London dashboard, London planner, SP15 dashboard, SP15 planner and simulator. All returned HTTP 200. Live SP15 returned 96 of 96 fully scored half-hours. London contained a ten-period upstream price gap; the longest complete contiguous segment was 62 periods, and missing intervals were rejected rather than treated as zero-cost.

### 2026-08-09 — session 6, checkpoint 3
- Carried the selected market/location through all three pages. Grid, Simulator and Planner now link to the corresponding view rather than resetting to GB national. The simulator receives market-specific currency, location, signal mode, price label and carbon label.
- Verified `/simulator?market=CAISO&location=sp15` through the local server: HTTP 200, dollar pricing, SP15 node metadata, six CAISO location options, custom-PNode entry and location-preserving navigation.
- Improved CAISO carbon provenance. The adapter now uses EIA-930's published consumption rate where present and a recent-period fuel-mix estimate otherwise. The fallback excludes imports and unclassified "other" generation rather than silently counting unknown generation as zero carbon. Unit tests cover both EIA parsing paths.
- Hardened `core/planner.py`: a gap inside a candidate window invalidates it; mixed-currency candidates are rejected until a conversion policy is explicit; Pareto marking is now a sorted `O(n log n)` scan. Added regression tests for all three behaviours.
- Added `docs/planner.md`, covering the exact optimisation equations, feasibility rules, explicit objective score, deterministic tie-breaking, Pareto definition, GB versus CAISO precision, primary official data references, complexity and production limits. Updated the README for the three-page product and US routes.
- Full configured-environment suite at this checkpoint: 54 passing; generated Python bytecode and JavaScript parser checks pass; the diff has no whitespace errors.

### 2026-08-10 — session 6, checkpoint 4
- Added `hardware/providers.py` with a real provider interface, read-only local detection and a simulated JSON fleet provider. Apple detection uses `system_profiler`; NVIDIA detection uses `nvidia-smi` when present. Public detection results deliberately omit serial numbers, UUIDs and host identifiers. Identity, memory, live power and performance each carry separate provenance.
- Verified local detection on Clyde's Mac: Apple M2, one device, 8 GB memory measured, eight GPU cores reported. Throughput and power remain ESTIMATED/UNAVAILABLE rather than being promoted by association. The simulator defaults to the detected device/count and overwrites only the measured memory capacity.
- Added `core/estimator.py`, the canonical Python workload estimator and bridge into `core/planner.py`. Training memory now defaults to 16 bytes/parameter plus 20% activation/buffer reserve, with sharded versus replicated feasibility. Inference weights and KV cache use independent precision controls. PUE, system efficiency and topology loss are explicit.
- Corrected the task-difficulty design in current documentation and added `core/routing.py`. The quality router requires comparable scores from one named/versioned evaluation suite, rejects estimated quality by default, enforces the quality floor first, then passes eligible model deployments to the exact placement planner. Added `docs/routing.md`.
- Added a local versioned integration surface: `/api/v1/health`, `/api/v1/market` and `/api/v1/plan`. The plan endpoint accepts validated workload/planning/device scope, uses the canonical estimator, and returns an auditable exact plan with provenance and alternatives. Added a shared data cache so Planner, Simulator and API do not refetch the same market/location replay independently.
- Live-tested the API on GB London: health returned version 0.3.0, market returned 96 periods with 86 complete, and a two-device planning request returned an exact H100 placement with 76 feasible alternatives and complete cost/carbon fields.
- Added planner exports for a review URL, plan JSON and alternatives CSV. Added `docs/api.md` and updated README.
- Full configured-environment suite at this checkpoint: 71 passing; compile checks and diff whitespace validation pass.

### 2026-08-10 — session 6, checkpoint 5
- Added `core/backtest.py` with a strict information boundary. Optimisation runs on forecast candidates captured at decision time; the chosen candidate and start are then replayed unchanged against realised signals.
- Backtest output now separates realised savings versus immediate execution, forecast cost/carbon error and perfect-hindsight cost/carbon regret. The realised oracle is reported for evaluation only and never influences the historical decision.
- Added failure handling for a missing selected timestamp or incomplete required realised signal. Candidate keys must remain unique across forecast and realised sets.
- Full configured-environment suite at this checkpoint: 74 passing. Persisted forecast snapshots and automatic outturn ingestion remain outstanding, so current UI claims still use measured historical replays rather than contractual backtest results.

### 2026-08-10 — session 6, checkpoint 6
- Added `core/audit_store.py`, a local SQLite audit log. Each API plan now persists a UUID, creation time, market/location, signal mode, exact request, exact response and the full price/carbon snapshot used to decide.
- Added score attachment for later realised backtest results plus list/retrieve APIs at `/api/v1/decisions` and `/api/v1/decisions/{id}`. SQLite foreign keys and WAL are enabled; writes are transactional.
- Live-tested plan creation, retrieval and listing on GB London. The stored record contained the full request/response and all 96 decision-time signals. The test-only audit database was moved to Trash afterwards so no fabricated operational record remains.
- Full configured-environment suite at this checkpoint: 77 passing. Automatic realised-outturn ingestion and scoring are the remaining parts of the evidence loop.

### 2026-08-10 — session 6, checkpoint 7
- Added `/api/v1/decisions/{id}/score`. A caller submits realised timestamp/price/carbon points; the service reconstructs the original candidates from the stored request and forecast snapshot, keeps the selected start fixed, calculates realised baseline/error/regret, and attaches the score without changing the original decision.
- Added "Save audited decision" to the planner. It sends the current visible constraints to the canonical Python estimator and exact planner, persists the result, and returns a linked decision UUID. Browser-side calculations remain responsive previews; the saved server result is authoritative.
- Hardened local HTTP responses with content security policy, frame denial, no-referrer, nosniff and no-store headers. JSON POST endpoints require `application/json` and enforce the 64 KiB body limit.
- Added an offline local HTTP harness that starts the real threaded server on an ephemeral loopback port. It verifies health/security headers, plan persistence/retrieval and content-type rejection without calling external data services.
- Full configured-environment suite at this checkpoint: 82 passing in 10.21 seconds; compile checks and diff whitespace validation pass.

### 2026-08-10 — session 6, checkpoint 8
- Added `/decisions`, an operator-facing audit journal backed by the versioned decision API. It presents saved recommendations, forecast-versus-realised evidence status, the chosen hardware/location/time, original constraints and signal boundary without mutating the source record.
- Added search and evidence-state filtering, keyboard-operable rows, JSON export and a detailed realised-outcome panel. Decision summaries are derived server-side so the journal does not fetch every full signal snapshot merely to render the table.
- Linked Grid, Simulator, Planner and Decisions in both directions. The journal itself does not fetch market data and remains usable when an upstream grid API is unavailable.
- Focused page, persistence and real-HTTP checks at this checkpoint: 24 passing. Full-suite verification follows after the remaining documentation and readiness work.

### 2026-08-10 — session 6, checkpoint 9
- Added `hardware/calibration.py`. Three or more observations with one exact device/model/task/precision/count/software-stack fingerprint are aggregated using median throughput and power; normalised median absolute deviation records run-to-run variation. Under-sampled, mixed or malformed groups fail closed.
- Wired the newest exact matching calibration into the canonical server estimator. A match promotes the runtime/power path to MEASURED and adds sample count, stack and robust variation to the decision assumptions. Any fingerprint mismatch retains the catalogue estimate.
- Added a local observation-to-profile command and excluded generated calibration profiles from version control. `docs/calibration.md` defines the input, evidence threshold and telemetry boundary.
- Planning responses now persist an untruncated feasible-candidate snapshot. Realised scoring reconstructs from that frozen snapshot, so a later catalogue edit or calibration change cannot alter the original decision or its hindsight comparison.
- Added `docs/commercial-readiness.md`, separating the current sellable pilot foundation from production and valuation claims, with explicit exit criteria for data, calibration, execution, security, reliability and deployment.
- Full configured-environment suite at this checkpoint: 89 passing in 3.84 seconds. Final compile, whitespace and HTTP smoke checks still follow.

### 2026-08-10 — session 6, checkpoint 10
- Added hard operating policies to `PlanningRequest` and the Planner page: maximum cost, maximum carbon and latest-start delay. These are feasibility constraints applied before min-max normalisation, so a soft objective weight cannot override an explicit budget or emissions ceiling.
- Hardened workload and objective inputs against non-finite values, booleans masquerading as numeric counts, excessive dimensions and duplicate device keys. Invalid requests fail with a bounded validation error rather than reaching ranking.
- Promoted the local product version to 0.4.0 while retaining the v1 API contract. Updated the algorithm, API, calibration and commercial-readiness documents to match the implemented evidence boundary.
- Final configured-environment suite: 99 passing in 3.92 seconds. Python compilation, generated JavaScript parsing and diff whitespace validation all pass. No commit or remote deployment was requested or performed.

### 2026-08-10 — session 7
- Started the current 0.4.0 product locally on port 8766 after discovering that port 8765 was occupied by an older server that did not expose the v1 health route.
- Verified `/api/v1/health` and a real market-backed Grid page response, then opened `http://localhost:8766`. The process remains loopback-only and is not remotely deployed.

### 2026-08-10 — session 8
- Added one shared Light/Dark slider to Grid, Simulator, Planner and Decisions. It is keyboard accessible, persists through local storage, follows the system preference until explicitly changed, and can override a dark or light operating-system setting.
- Added `adapters/nyiso.py` and exposed New York ISO under a United States market group. Eleven official NYISO price zones now join keyless day-ahead zonal LBMP with EIA-930 balancing-area carbon. Daylight-saving fall-back hours remain distinct.
- Live-verified the New York City zone: 96 of 96 half-hours complete, $43.54 to $151.00/MWh and 274.69 to 309.89 gCO2/kWh across the replay. Carbon used the clearly labelled EIA fuel-mix fallback and was not presented as zonal.
- Added a native one-click macOS launcher on the Desktop. It reuses a healthy process or starts the server on a free loopback port, waits for `/api/v1/health`, opens the product and records only local runtime state under `.runtime/`. A repeated invocation reused the same PID set.
- Product version is now 0.5.0. Full configured-environment suite: 104 passing in 5.99 seconds; compilation, generated JavaScript parsing and diff whitespace validation pass.

### 2026-08-10 — session 9
- Replaced the old compact-panel expansion, which only stretched static SVG artwork, with a reusable high-DPI analytical workbench. It supports independent X/Y zoom, wheel and keyboard control, pointer/touch pan, shift-drag box selection, crosshair readouts, series toggles, grid and logarithmic-Y modes, exact-data tables, CSV export and PNG export.
- Added structured inspector contracts to all seven compact Grid analytics, with explicit units, bands, guides and distinct series. The Planner Pareto view now exposes feasible, frontier and selected placements with exact cost, carbon, hardware and UTC placement details. Simulator renewables use the same numerical inspector; the allocation flow has a diagram-specific pan/zoom and SVG-export workspace.
- Upgraded the primary price and carbon charts with state-preserving full screen, denser horizontal levels, vertical time guides, switchable grid and crosshair controls, a persistent labelled latest-value line, floating exact timestamp/value badges, independent Y zoom and Y pan, keyboard control and focus containment. The existing independent range/interval selection, OHLC, candles, bars, overlays, X zoom/pan, box selection and CSV export remain intact.
- Live visual QA against the launcher-managed product caught three issues automated structure checks did not: the appearance switch covered the compact Close control, undefined dashboard colour variables made some series black or identical, and a hidden spread sub-chart still consumed space. All three were corrected. Live Grid full screen showed 17 grid guides, one labelled latest-value line and no inactive spread pane; the compact and Planner workbenches opened with the expected controls and series, with no browser console warnings or errors.
- A final route smoke test found that Simulator could block for more than 45 seconds while six optional weather and regional-carbon requests ran serially. Site signals now refresh asynchronously behind `/api/v1/sites`, the Simulator renders immediately with an explicit loading/unavailable state, and bounded concurrent upstream calls populate the page without blocking hardware or grid calculations.
- Product version is now 0.6.0. The final Desktop relaunch is healthy on `http://localhost:8766`, PID 1676. Cold Simulator response fell from a timeout beyond 45 seconds to 1.33 seconds; Planner, Decisions, site signals and health then returned in under 4 ms from local caches, with all six site forecasts populated. The full configured-environment suite is 107 passing; compilation, generated JavaScript parsing and diff whitespace validation pass.

### 2026-08-10 — session 10

- Confirmed the primary user as an operator managing AI data-centre workloads, with Apple Intelligence and Private Cloud Compute as the first presentation context and an architecture-agnostic core.
- Confirmed energy consumption as the central operational outcome and retained regional electricity price and carbon intensity as first-class scheduling inputs.
- Corrected the optimisation framing: workload measurements establish how much quality-qualified work an execution option can complete and how much energy it consumes; grid signals decide where and when that work should run.
- Set an eight-day target using the available M2 Mac, iPhone and iPad. Exact mobile device models and operating-system versions are still required before the native measurement plan can be finalised.
- Chose a privacy-preserving evidence direction: store workload class, model/version, precision, shapes/counts, device/runtime configuration, duration, energy, memory, thermal state and task-quality score; do not upload raw prompts, audio, images or user content.
- Reviewed the local measurement environment. Xcode and `xctrace` are present; the MLX and Core ML benchmark dependencies still need to be installed and pinned.
- Added a modality-neutral workload evidence contract and robust exact-fingerprint aggregation. The schema contains no fields for prompts, text, images, audio, labels or customer content.
- Added an exact bounded portfolio scheduler for multiple AI jobs and constrained facilities. It maximises explicit operator utility, then minimises operational carbon, electricity cost and delay while enforcing deadlines, half-hour capacity and hard total carbon/cost budgets.
- Added eleven focused tests across the evidence and portfolio modules; all pass. Full-suite verification and application/API integration remain next.
- Added `/workloads`, a fifth linked operator page for facility power/cost/carbon policies and a heterogeneous AI workload queue. It clearly labels its starter values as estimated scenarios, accepts measured evidence state only when the operator selects it, submits to the canonical portfolio API, and exports plan JSON or schedule CSV.
- Added `POST /api/v1/portfolio`. It applies a measured-quality gate by default, validates explicit variant runtime/power/PUE/provenance, uses the selected market/location series, enforces the facility boundary and returns exact queue totals and assignments.
- Browser QA found that portfolio `PlanOption.score` remained infinite because portfolio ranking is lexicographic rather than the single-plan weighted score. The server emitted non-standard `Infinity`, which the browser rejected. The shared serializer now emits `null` for a non-applicable score and all JSON responses fail closed on any remaining NaN/infinity.
- Promoted the product to 0.7.0 and restarted through the Desktop launcher. It is live at `http://localhost:8767`, PID 2628. Grid, Simulator, Planner, Workloads, Decisions and health all return HTTP 200.
- Final verification: **124 passing** in 6.99 seconds, Python compilation passes, generated JavaScript parsing passes and the diff has no whitespace errors.
- Device discovery identified the paired phone as an iPhone 16 Plus on iOS 26.5. It is currently offline. No physical iPad is paired. The exact iPad model/OS and ability to connect both mobile devices in Developer Mode remain blocking inputs for native collection.
- Added and documented the Apple measurement protocol plus a pinned optional dependency set. Model downloads remain paused because only about 17 GiB is free and the first evidence set should stay below a 2 GiB checkpoint budget.

### 2026-08-10 — session 11

- Clyde identified that the 0.7.0 interface still presents the product backwards: grid information leads, while the intended operator begins with AI data-centre workload and optimisation state.
- Set the active redesign boundary: make AI operations the home page; lead with workload demand, quality/SLA gates, fleet and facility capacity, evidence state and recommended execution; move detailed grid analytics to a linked Sites & Grid specialist view.
- Grid price and regional carbon remain first-class algorithm inputs. This is an information hierarchy correction, not a removal or demotion of market data from optimisation.
- Inverted the live route hierarchy. The Desktop launcher now opens `/` as AI Data Centre Operations. The complete graphing terminal moved to `/grid`; `/workloads` remains an alias. Navigation is now Operations, Fleet Lab, Placement Lab, Sites & Grid and Decisions, with market/location preserved.
- Rebuilt the landing information hierarchy around an operator control plane: queued workloads, mandatory demand, measured-profile coverage, deadline range and decision state lead; the workload admission queue follows; quality/memory/SLA/capacity preflight runs next; facility price/carbon policy appears as placement constraints afterwards.
- Extended AI execution inputs and portfolio responses with workload class, run mode, model/version, precision, compute unit, declared memory required/available, quality suite and provenance. Memory fit now rejects infeasible candidates before grid ranking.
- Added useful-work efficiency outputs to each assignment: Wh per work unit, grams CO2 per work unit and cost per work unit, while retaining aggregate facility energy, cost and carbon.
- Promoted the product to 0.8.0 and restarted it through the Desktop launcher at `http://localhost:8766`, PID 3277. All live operator routes and health return HTTP 200.
- Final verification: **126 passing** in 6.96 seconds, Python compilation passes, generated JavaScript parsing passes and the diff has no whitespace errors.

### 2026-08-10 — session 12

- Clyde specified the next AI Operations loop: retain compact regional grid price/carbon context on the home surface, provide example AI workflows decomposed into stages, and show those stages running at different times as the operator changes the allowed timeframe.
- Active algorithm work: add explicit stage dependencies to the exact portfolio scheduler so preprocessing, inference/training, evaluation and reporting cannot be reordered merely because a later grid interval is cheaper or cleaner.
- Active interface work: add a global planning timeframe, example workflow templates, a compact grid signal strip with source/location context and a visible run schedule that updates from the canonical portfolio API.

### 2026-08-10 — session 13

- Completed the stage-dependency path across the exact portfolio scheduler, portfolio API and AI Operations page. Jobs now accept a workflow ID, stage name and dependency IDs; unknown dependencies, duplicate IDs, self-dependencies and cycles fail closed; a dependent stage cannot start until every selected predecessor has finished.
- Added three editable example workflows for language evaluation, vision validation and speech regression. Each decomposes useful AI work into preparation, execution and evaluation/reporting stages with explicit dependencies.
- Added a global 6, 12, 24 or 48 hour completion window. Loading a workflow or changing this control updates every stage deadline before the canonical portfolio API optimises the run plan.
- Retained grid data inside the AI-first home surface as placement context. The selected market/location, provenance mode, first-interval price and carbon, lowest price, cleanest interval and 30-minute signal resolution are visible beside facility constraints; the full terminal remains linked under Sites & Grid.
- Added the generated local runtime directory to `.gitignore`. No local process state, cache, audit database, calibration profile or device identifier is included in the release scope.
- Installed the official checksum-verified GitHub CLI 2.97.0 ARM64 build at `~/.local/bin/gh`, added that user directory to future login-shell paths and authenticated the `ClydeLartey97` account. The remote repository is `ClydeLartey97/AI-Energy`, whose default branch is `main`.
- Release-candidate verification: **129 passing** in 7.84 seconds, Python compilation passes and the diff has no whitespace errors. Publication was requested directly to `main`; commit and push follow this checkpoint.

### 2026-08-10 — session 14

- Clyde clarified that alignment with data-centre operators' net-zero goals is a central commercial requirement, not a secondary dashboard feature.
- Product framing: the system is an AI workload control plane that decomposes flexible workflows into quality- and deadline-constrained stages, then schedules eligible execution across hardware, location and time using electricity price, regional carbon intensity and generation-derived grid conditions.
- The environmental claim must remain precise. The present system supports carbon-aware operational scheduling and hard carbon budgets; it does not yet prove net-zero operation, 24/7 carbon-free-energy matching, avoided emissions or carbon-credit eligibility.
- Required next environmental layer: generation and renewable-availability forecasts, marginal-emissions signals where defensible, curtailment awareness, customer energy-contract context, and separate location-based, market-based and 24/7 CFE reporting. This must preserve workload quality, dependency and SLA constraints rather than optimising carbon in isolation.

### 2026-08-10 — session 15

- Clyde elevated electricity generation and direct renewable supply to a first-class optimisation dimension alongside electricity price. The target is joint optimisation of AI workload execution and the energy supply available to each facility.
- Required energy-supply model: forecast output from onsite or contracted renewable plants; behind-the-meter generation; dedicated interconnection limits; grid imports and exports; battery charge, discharge and state of charge; power-purchase agreement delivery; curtailment; and residual grid price/carbon.
- The optimiser must enforce an interval energy balance: facility demand plus battery charging equals renewable delivery plus grid import plus battery discharge, subject to compute capacity, workflow dependency, quality and SLA constraints.
- Provenance must distinguish physical renewable delivery from contractual matching. Onsite or dedicated-wire generation is not the same claim as a grid-delivered PPA, virtual PPA, renewable certificate or annual renewable matching. The interface and audit record must never collapse these boundaries into one "renewable" label.
- Commercial framing: synchronise flexible AI computation with verifiably available clean generation, using grid power only for the residual requirement and retaining price, reliability and emissions evidence for every interval.
- Canonical demonstration scenario: an energy-intensive, high-difficulty AI task with a flexible deadline is placed into the facility's forecast solar-surplus window. Preprocessing may run earlier; the accelerator-heavy stage starts when usable solar output is highest; evaluation and reporting can follow later. Placement remains conditional on quality, memory, hardware capacity, dependency completion, forecast confidence and the task deadline.
- "Peak sunlight" must be implemented as peak usable renewable surplus rather than clock time alone. The calculation must subtract other facility demand, respect interconnection and battery limits, account for temperature-related cooling/PUE effects, and retain a grid or battery fallback when generation underperforms.

### 2026-08-10 — session 16

- Clyde's primary Apple outcome is now explicit: present the work as an undergraduate Computational Economics project strong enough to earn a technical referral, interview, internship or early-career role where he can continue working on AI infrastructure and energy optimisation.
- Relevant team intersections include Apple Energy Services, Machine Learning Infrastructure, Apple Intelligence infrastructure, cloud/data infrastructure and data-centre hardware efficiency. The project should be presented as evidence of systems thinking and an experimentally testable thesis, not as a claim that Apple lacks an internal scheduler.
- The presentation must separate what is implemented, what is measured, what is simulated and the proposed joint workload/renewable-dispatch layer. The strongest hiring signal will be a live, technically resilient demonstration plus a concise discussion of algorithmic constraints, evidence boundaries, failure modes and the next production experiment.

### 2026-08-10 — session 17, generation-aware dispatch checkpoint

- Replaced the planned solar-specific layer with a source-agnostic physical energy model in `core/energy.py`. Supported source kinds are solar, wind, hydro, nuclear, geothermal, biomass, gas, coal, oil, residual grid and other. Each source carries interval availability, forecast confidence, marginal cost, carbon intensity, renewable and carbon-free classification, dispatchability, delivery type and provenance.
- Preserved the claim boundary between physical delivery and contractual matching. Onsite, dedicated-wire and grid sources can serve facility demand; a contractual source is reported as available but cannot satisfy the physical interval balance.
- Implemented confidence derating, fixed base-facility demand before flexible AI demand, interval PUE, residual-grid fallback, renewable/carbon-free curtailment accounting and battery charge/discharge with round-trip losses and stored-source attributes. Battery energy charged from residual clean supply may be explicitly reserved for later controllable AI demand; this is an operating attribution policy, not a claim that electrons are distinguishable.
- Integrated energy dispatch into the exact workflow placement search. Every complete legal schedule is evaluated against the same site dispatch model; infeasible supply is rejected; hard facility, workflow, quality, memory, deadline, cost and carbon limits remain in force.
- Corrected the energy ranking to compare renewable and carbon-free match percentages rather than absolute matched kWh. This prevents a higher-PUE interval from winning merely because it consumes more energy. Operators can select renewable-first, carbon-free-first, operational-carbon-first or electricity-cost-first lexicographic policy.
- Aligned source and battery merit order with that selected policy. Renewable-first prioritises renewable dispatchable supply, carbon-free-first prioritises carbon-free supply, carbon-first uses operational intensity, and cost-first uses marginal cost. The setting now changes both workload placement and the physical source allocation used to score it.
- Added a deterministic earliest-feasible counterfactual that holds admitted work, quality, dependencies, deadlines and facility capacity constant but starts work as soon as possible without energy optimisation. API output reports schedule delay, PUE-related energy difference, cost, carbon, renewable/CFE uplift and residual-grid energy avoided.
- Added checkpoint expansion for explicitly checkpointable jobs. One declared job can be split into up to 24 ordered restart chunks, allowing the exact scheduler to place chunks across separate clean-generation windows. Non-checkpointable work remains one continuous interval; unapproved splitting fails closed.
- Rebuilt the AI Operations energy surface. It now exposes all source types with capacity, confidence, cost, carbon and delivery controls; base demand; daytime/night PUE; solar peak; storage size, charge/discharge limits and efficiency; an operator-selectable energy objective; and an estimated generation-aware training workflow with a 50 kW accelerator stage.
- Added expandable, fully interactive generation, interval-dispatch and source-mix charts plus assignment-level renewable, carbon-free, grid, battery, cost and carbon results, source accounting and the earliest-run comparison. The standard generation shapes are visibly labelled ESTIMATED and must be replaced by plant/operator forecasts before a real operating claim.
- Live browser QA exercised the default workflow, light/dark themes, mobile width, full-screen graph expansion, exact-value tables and close-state restoration. It caught and fixed two UI/data-path issues: incomplete market intervals no longer block unrelated base-load accounting while AI placement still fails closed on incomplete signals, and a closed full-screen chart no longer leaves its workbench inside the compact card.
- The default estimated scenario now demonstrates a non-trivial decision: moving workflow completion by 2.5 hours raises renewable matching from 48.3% to 90.9%, avoids 51.29 kWh of residual grid electricity and 1.369 kg operational carbon, and saves £1.175 under the replay and declared generation profile. These are scenario results, not realised savings.
- Final configured-environment verification: **148 passing**. Python compilation, generated JavaScript parsing and diff whitespace validation pass. Product version `0.9.0` is running loopback-only through the Desktop launcher; final PID/port are recorded in the most recent session entry below.

### 2026-08-10 — session 18, live 0.9.0 handoff

- The final launcher-managed process is healthy at `http://localhost:8766`, PID `6480`. `/api/v1/health` reports product `0.9.0`, API `v1` and `local_only: true`.
- The default Operations scenario uses a 12-hour window, 60 kW base facility load, a 50 kW two-hour accelerator stage, estimated solar/wind profiles and no initial storage. A live run completed successfully with the scenario comparison recorded above.
- Full-screen dispatch inspection was exercised live: X/Y zoom, grid, crosshair, series legend, exact data table, CSV and PNG controls mounted; closing returned to the compact line chart and hid the workbench correctly. The browser console contained no warnings or errors.
- Light/dark operation and a 390 × 844 mobile viewport were checked after the final fixes. The page had no horizontal document overflow; the mobile appearance switch remains at the page top and no longer covers scrolled form controls.
- The implementation and documentation changes remain local and uncommitted at this handoff. The last remote commit predates the 0.9.0 generation-dispatch work; do not claim GitHub contains this version until it is explicitly committed and pushed to `main`.

### 2026-08-10 — session 19, exact site and source geometry checkpoint

- Clyde required unrestricted physical-location precision. The implementation now separates exact operator-declared WGS84 facility/source coordinates and grid-connection identities from the coarser spatial scope of provider price and carbon signals. It does not relabel national, regional, zonal or balancing-area data as facility-level measurement.
- Added validated facility identity, latitude, longitude, grid connection and IANA time zone to the canonical energy profile. Added optional coordinates, connection identity and declared delivery-loss fraction to every energy source.
- The dispatch core now calculates auditable great-circle source-to-site distance, derates physical supply by the declared delivery loss before it can serve demand, and reports lost kWh by source. Contractual-only instruments remain outside the physical balance.
- Portfolio API responses now include a structured spatial-precision boundary describing exact physical-site scope, provider price scope, provider carbon scope and 30-minute temporal grain. Product version is moving to `0.10.0`; the geospatial energy algorithm identifier is `exact-workflow-with-geospatial-energy-dispatch-v3`.
- The Operations page now accepts an exact facility site ID, name, latitude, longitude, connection/meter ID and IANA time zone. Each source row accepts origin coordinates, connection ID and declared delivery-loss percentage. Results show source origin, great-circle distance and percentage/kWh loss.
- The market picker now names price and carbon scopes independently. CAISO additionally accepts an arbitrary valid PNode from the primary Operations surface; GB remains national-price plus national/regional-carbon, and NYISO remains zonal-price plus balancing-area-carbon.
- README, API, dispatch and commercial-readiness documentation now define the geospatial model and its claim boundary. Focused core, API and page verification is **57 passing**; compilation and diff whitespace validation pass. Full-suite verification, live browser QA and launcher restart remain active work. Changes are still local and uncommitted.
- Corrected temporal-granularity labelling during live review: the optimiser retains a 30-minute decision interval, while `spatial_precision` separately reports the provider-native resolution. Current CAISO and NYISO source observations are hourly and expanded across two decision intervals; current GB inputs are half-hourly.
- Final live UI verification entered facility coordinates 51.507400, -0.127800 and solar-source coordinates 51.900000, -0.127800 with an 8% declared delivery loss. The source account reported 43.66 km and 131.821 kWh lost across the scenario horizon before dispatch. The complete workload remained schedulable; renewable matching was 80.1% versus 34.6% in the earliest-run counterfactual, with 54.77 kWh less residual grid energy under the declared scenario.
- The expandable scheduled-dispatch chart was reopened after optimisation and exposed Fit, X/Y zoom, grid, crosshair, logarithmic Y, exact data, CSV, PNG and series visibility controls without breaking the location result.
- Final verification is **153 passing** in 7.50 seconds. Compilation and diff whitespace validation pass. Product `0.10.0` is healthy through the Desktop launcher at `http://localhost:8766`, PID `7145`, loopback-only. The current implementation remains local and uncommitted; the remote repository still does not contain the 0.9.0 or 0.10.0 work.

### 2026-08-10 — session 20, measured Apple-silicon evidence checkpoint

- The next commercial gap is now the active workstream: replace editable ESTIMATED workload profiles with repeatable, privacy-preserving measurements that the optimiser can consume without trusting browser fields.
- Installed the pinned optional profiling environment into `~/venvs/national-grid`: MLX 0.32.0, MLX-LM 0.31.3, Core ML Tools 9.0 and psutil 7.2.2. No model weights or evaluation data were downloaded. The installation does not change the public repository.
- Confirmed that `powermetrics` is available but privileged sampling requires a user password. The collector must therefore fail closed when that privilege is absent, and may accept a calibrated external meter value. It must never convert runtime, battery percentage or Energy Impact into watt-hours.
- Extended `workload-evidence-v1` with explicit energy method, measurement scope and energy provenance. External meter, Apple subsystem estimate and integrated NVIDIA board-power methods have fixed compatible scopes; invalid promotion is rejected. Apple `powermetrics` profiles are explicitly restricted from cross-device energy comparison.
- Added an append-only local SQLite evidence store under the ignored `data/cache/` path. Run IDs are immutable and idempotent, serious/critical thermal runs are rejected, and only three or more exact-fingerprint observations produce a stable measured profile ID. Focused evidence verification is **9 passing**.
- API integration, benchmark execution, Operations evidence controls, full-suite verification and launcher restart remain active work. Changes are local and uncommitted.

### 2026-08-10 — session 21, live governed-evidence integration

- Added `hardware/apple_benchmark.py`, a privacy-preserving Apple-silicon command collector with metadata-only result JSON, measured runtime, peak process-tree RSS and exact energy-method enforcement. Apple `powermetrics` runs fail closed without prior authorisation; calibrated external device-input energy is the cross-device method.
- An actual MLX matrix probe completed on the local M2 and confirmed the installed accelerator execution path. It remains performance-only and did not create fake scheduling evidence because it had no task-quality result or watt-hour measurement.
- Added `GET /api/v1/evidence/profiles` and `POST /api/v1/evidence/observations`. The portfolio API accepts a stable evidence profile ID, derives model/hardware/runtime/power/memory/quality fields server-side, rejects conflicting client values and prevents Apple subsystem estimates from being ranked against a different device.
- Added the Operations evidence registry and per-workload governed profile selector. The empty state is honest: the live store currently contains zero observations and zero profiles because no reference model or valid energy run has been recorded.
- Live browser testing caught a full-screen interaction defect specific to a chart inside a sticky layout. Expanded charts now move into a top-level portal and return to their original DOM position on close. Grid toggling, exact-data table display, close restoration, theme control and cross-page navigation were exercised successfully.
- Final verification is **163 passing**. Diff whitespace validation passes. Product `0.11.0` is healthy through the Desktop launcher at `http://localhost:8766`, PID `8260`, loopback-only. The implementation remains local and uncommitted; the remote repository does not contain this version.

### 2026-08-10 — session 22, automatic model routing and reference workload

- Added automatic governed-profile expansion. An Operations job can request every compatible stored profile; the server derives candidates and jointly selects quality-qualified model, precision, hardware, time and energy supply. Response evidence now includes eligible variant count by job.
- Closed two comparison errors before live evidence exists: scores from different evaluation-suite versions cannot be ranked, and same-device Apple subsystem estimates cannot be mixed with another device, manual estimate or different measurement scope. Calibrated external device-input profiles remain cross-device comparable.
- Added the first executable reference plug-in, `hardware.reference_language`. It runs a deterministic public ten-item MLX language evaluation, pins the model to a 40-character immutable commit, hashes the dataset into the suite and shape fingerprint, retains generated text in memory only, and reports fixed evaluated samples plus measured accuracy.
- Tightened the collector result contract to exactly three aggregate fields. Added a post-run external-meter prompt so the operator can read interval watt-hours after the workload finishes instead of supplying an impossible future value before execution.
- The Operations evidence card now reports installed reference collectors as well as observations and profiles. The first runner appears as ready while the live evidence store truthfully remains empty.
- Downloaded and evaluated pinned `mlx-community/Qwen2.5-0.5B-Instruct-4bit` revision `a5339a4131f135d0fdc6a5c8b5bbed2753bbe0f3`. It executed on MLX but scored only 0.40 on the smoke suite, so it was not promoted to scheduler evidence. A stronger official 1.7B pinned model is being downloaded within the declared 2 GiB model-set ceiling.
- Verification at this checkpoint is **171 passing**, with Python compilation and diff whitespace validation passing. Code is moving to product `0.12.0`; the launcher still serves the prior `0.11.0` process until final restart. Changes remain local and uncommitted.

### 2026-08-10/11 — session 23, product redesign (in progress — checkpoint 1)

- Discovered and recorded the git-state drift described in Current State above: commit `a474b20` was already pushed to `origin/main` outside a logged session, covering through session 16; sessions 17-22's work was still local-only. Clyde separately asked for everything outstanding to be pushed — this is queued as a to-do for this session, not yet done as of this checkpoint.
- Took the redesign brief literally rather than guessing: read `app/theme.py`, `app/chart.py`, and screenshotted the live 0.12.0 product (Operations, Grid, Simulator, Planner) with headless Chrome before changing anything, to identify the actual "looks like Codex" signature rather than a generic one. Findings: pure-black/one-blue-accent palette is fine (it's genuine Apple system colour, validated earlier); the tell is structural — `border-radius:980-999px` used blanket on nav tabs, toolbar segmented controls and buttons (not just genuine pill badges), every card/tile sharing identical flat chrome with no depth hierarchy, shadow-pop hover on cards, and a small-caps `.eyebrow`/`.ops-stat span`/`.chain-step`/`.pnl h3` kicker-label-above-headline pattern repeated on nearly every section (`app/workloads.py`, `app/decisions.py`, `app/dashboard.py`). Table headers and toolbar group labels (`.ch-lbl`) were deliberately left uppercase — that's a normal dense-UI convention, not the tell.
- Rebuilt `app/chart.py` in full — see Current State for the feature list (Baseline/Histogram/Heatmap types, Bollinger, log-Y, %Δ, floating OHLC tooltip, de-pilled toolbar). Verified three ways, in order of trust: `node --check` on the extracted script (syntax only), the full `tests/` suite (172 passing, includes the JS-parses-in-a-real-engine and no-leftover-`__TOKEN__` guards), then rendering the chart standalone in headless Chrome with a small self-clicking test harness (`window.onload` → `setTimeout` → `.click()` on each new button) and screenshotting every mode — this last step is what actually caught the `.hidden` bug; the first two passed cleanly with the bug still present, which is itself worth remembering next time a "toggle doesn't seem to do anything" report comes in.
- Still open when this checkpoint was written, in priority order: (1) push everything to `origin/main`, (2) carry the de-pill/de-eyebrow/hover restyle from `chart.py` into `app/theme.py`, `app/dashboard.py`, `app/panels.py`, `app/planner.py`, `app/simulator.py`, `app/workloads.py`, `app/decisions.py` — each of those files owns its own copy of `nav a`, `.card`, button and hover rules rather than sharing one module, so this is a per-file pass, not a one-line fix, (3) regroup the ~24 flat fields on each Operations queue-item card into labelled subsections, (4) re-screenshot every page light/dark after the restyle, (5) full suite + `node --check` on every page's generated JS one more time, (6) commit and push. If this file says "in progress" in its own next-steps list and nobody has appended a later checkpoint entry above this one, treat all six as still outstanding and pick up at (1).

### 2026-08-10/11 — session 23, product redesign (checkpoint 2 — restyle and density complete, pushed)

- Completed items (2) through (5) from checkpoint 1's list above — see the redesign bullets added to Current State for the exact detail (nav segmented-control pattern, de-capped kicker labels, tint-not-shadow hover, divider-grid decision chain, five collapsible field groups per Operations queue item). All done as targeted CSS/markup edits within each page's existing structure, not a shared-module refactor — each page still owns its own `<style>` block; a future session could still extract a shared `app/style.py` if the duplication becomes a real maintenance cost, but it wasn't necessary to hit the actual ask.
- Final verification before pushing: **172 passing** (unchanged — the redesign touched presentation, not logic), `py_compile` clean across `app/`, `core/`, `adapters/`, `hardware/`, no whitespace errors in the diff, and direct visual confirmation via headless-Chrome screenshots of Operations/Grid/Fleet Lab/Placement Lab in dark and Operations in light.
- Completed item (1): committed and pushed **all** outstanding work to `origin/main` in one sitting — both the sessions 17-22 backlog (generation-aware energy dispatch, geospatial site/source coordinates, the Apple-silicon evidence pipeline, automatic model routing) that had been local-only since 11:53 that morning, and this session's redesign on top. Exact commit(s) and the resulting `origin/main` HEAD are recorded by whichever tool ran the push — check `git log origin/main -3` rather than trusting a number written here, since this line cannot update itself after the fact.
- Not done this session, left for next time: PNG export on the main chart terminal (see the scope-cut note in Current State); auditing `app/panels.py`, `app/dashboard.py`, `app/flow.py`, `app/kline.py` for the same SVG-`.hidden` reflection bug found and fixed in `chart.py` (grep `\.hidden\s*=` in each — nothing found yet means nobody's looked, not that it's clean); the eight-day Apple-silicon evidence campaign (still zero real observations in the evidence store as of this session); a shared design-token module if per-page CSS duplication becomes painful to maintain.

### 2026-08-11 — session 24, facility discovery boundary design (no code)

- Scoped Next Steps item 19 design-first, per its own instruction: read `hardware/providers.py`, `hardware/base.py`, `hardware/calibration.py` and `docs/calibration.md` before writing anything, so the design fits the existing `HardwareProvider` interface and per-field provenance discipline rather than inventing a parallel one.
- Wrote `docs/discovery.md` — the discovery boundary, decided before any collector exists. Core structure: three access tiers (anonymous-by-standard reads / operator-supplied read-only credentials / never-automatic), with SNMP `public` deliberately classified as a credential rather than tier 0, and all writes, credential guessing, undeclared-address scanning and raw-identifier retention in the never tier.
- Design decisions recorded in the doc, each resolving a real tension: fleet dedupe needs stable device identity but the project bans retaining serials/UUIDs — resolved with an HMAC-SHA256 keyed digest under a local site key; PDU outlet, Redfish system and DCGM board power measure different things — resolved with a mandatory measurement-scope label mirroring `workload-evidence-v1` energy methods; auto-discovery must not inflate certainty — discovered identity/memory/live-power can be MEASURED but performance never rises above catalogue provenance, and an unmatched device stays visible as UNAVAILABLE rather than being dropped or invented.
- Proposed Redfish as the first collector, using the same selection logic that made GB the first market adapter: the clearest read-only path to a real signal (DSP0266 mandates an anonymous service root, so the safe/credentialed boundary comes from the standard itself), zero new dependencies (stdlib HTTPS+JSON — checked this machine: `snmpwalk`/`snmpget` present, `ipmitool`/`lldpcli`/`dcgmi`/`nmap` absent), and an honest four-stage verification ladder that doesn't require a data centre: DMTF DSP2043 mockup fixtures offline → the same tree over local HTTP → OpenBMC under QEMU (real BMC firmware, real session auth) → a physical BMC. DCGM, SNMP-PDU and LLDP follow in that order, each getting the same tier analysis before implementation.
- Recorded the honest hardware limit: no BMC, PDU, managed switch or NVIDIA GPU is reachable from the current dev machine, so ladder stages 1–3 are the ceiling here; stage 4 needs borrowed/rented hardware and nothing above stage 3 may be claimed before it runs.
- Deliberately wrote no collector code — the build is gated on Clyde reviewing `docs/discovery.md` first. Changes this session: `docs/discovery.md` (new), this file's Current State, item 19 and this entry. Local and uncommitted.

### 2026-08-11 — session 25, RedfishFleetProvider built and verified to ladder stage 2

- Clyde lifted the design-review gate immediately after session 24 ("is there something you can build") — recorded here as approval to proceed on the design as written.
- Fetched DMTF's `Redfish-Mockup-Server` repository (BSD-3-Clause) and pruned its real `public-rackmount1` mockup tree into `tests/fixtures/redfish/` — service root, one system, its Processors (2 CPUs + 1 FPGA), one chassis and its Power resource, each file retaining its original `@Redfish.Copyright` line, with a README naming the source. Real DMTF-published payload shapes, not hand-invented fixtures — the same pattern as verifying `gb_adapter` against its upstream's own fixtures.
- Implemented `RedfishFleetProvider` in `hardware/providers.py` to the boundary in `docs/discovery.md`: GET is the only verb in the module; anonymous access is limited to the DSP0266 service root and credentials are never sent to it; the credentialed walk (HTTP Basic from an operator-named environment variable) descends Systems → accelerator Processors → Chassis → Power; chassis input watts join to systems only through `PowerControl.RelatedItem`; a per-endpoint request budget fails closed to a partial inventory; plain HTTP is rejected off loopback at construction; TLS supports per-endpoint certificate-fingerprint pinning; and `from_config` parses `facility-discovery-v1`, rejecting unknown protocol names so a typo cannot silently drop an endpoint.
- Extended `DetectedDevice` with `power_scope` (board/system/outlet — a Redfish chassis reading is labelled `system` and noted as including fans/PSU losses) and `device_digest` (HMAC-SHA256 of the protocol-native durable ID under a site key auto-created at `data/cache/discovery-site-key`, gitignored). Raw serials, UUIDs, SKUs and asset tags are discarded at read time; tests assert none survive into `public_dict()` and that digests are stable under one key and different under another.
- Added nine offline tests to `tests/test_hardware_providers.py`: full mockup walk (Contoso 3500 with 96 GiB MEASURED memory, 344 W MEASURED system-scope power, UNAVAILABLE performance; Stratix 10 FPGA visible with UNAVAILABLE everything except MEASURED identity; CPUs summarised, never separate devices), identifier hygiene, digest keying, service-root-only without credentials, missing-credential-env fail-closed (warning names the variable, never the secret), loopback-only plain HTTP, request-budget partial failure, two-endpoint partial inventory when one host refuses, and config-loader protocol validation.
- **Ladder stage 2 verified live:** served the full mockup tree over loopback HTTP with a stdlib shim that returns 401 below the service root without an Authorization header, and ran the provider's real `http.client` transport against it — complete inventory, zero warnings, and a scripted leak check confirming no serial/UUID/SKU/asset-tag/credential string appears anywhere in the public output. Stages 3 (OpenBMC under QEMU) and 4 (physical BMC) remain open; nothing above stage 2 is claimed.
- Full configured-environment suite: **181 passing** (172 before this session; +9). `py_compile` clean across `hardware/`, `app/`, `core/`, `adapters/`; `git diff --check` clean. `docs/discovery.md` amended in the two places the implementation refined it (Basic-or-session auth wording; loopback-only `"tls": false` and the request budget in the config section). Changes local and uncommitted.

### 2026-08-11 — session 25 continued: discovery wired into the product

- Built the pipe item 19 exists for — discovered inventory flowing to the operator, not another collector. Added `hardware/inventory_store.py` (append-only SQLite snapshots of `DetectionResult.public_dict()` under ignored `data/cache/`; record, latest and summary only; a test asserts no update/delete API exists), `GET /api/v1/inventory` and `POST /api/v1/inventory/refresh` in `app/serve.py` (config at gitignored `data/discovery.json`, `DISCOVERY_CONFIG` override; 409 when unconfigured, 201 with the recorded snapshot), and the Operations "Discovered fleet inventory" card (per-device provenance, power scope, source endpoint, short keyed-digest fleet ID, Run discovery button, honest unconfigured/never-run empty states). Documented both endpoints in `docs/api.md`.
- Extended the offline server harness with a real loopback Redfish fixture server that enforces the auth boundary (401 below the anonymous service root), and covered: unconfigured honesty, 409 refresh, and a full walk over real HTTP asserting both device names and that no raw serial/UUID/SKU/asset-tag/credential survives the entire HTTP pipe. Suite: **187 passing** (+6).
- Live QA through the real product: started `app.serve` with a scratchpad discovery config pointing at the served DMTF mockup tree, exercised 409→201→snapshot over the API, then screenshotted the rendered Operations page in headless Chrome. The screenshot caught a real defect the tests could not: the card's stats row used the shared five-column `.ops-grid` with only four tiles, leaving an empty grey cell. Fixed (added the Access tier tile), restarted the server — a reminder that the running process does not see edits — and re-screenshotted clean. Deleted the QA-generated `inventory.sqlite` and site key afterwards so no fabricated device record persists.
- Clyde directed comprehensive US market coverage mid-session ("it is only 2 markets rn") — recorded as Next Steps item 20 with the keyless-first build order: ERCOT, then MISO/SPP, then keyed PJM/ISO-NE. ERCOT adapter work begins next.

### 2026-08-11 — session 25 continued: MISO becomes the third US-adjacent market (fourth overall)

- Measured reachability before writing any adapter, and it overturned the planned ERCOT-first order: ERCOT MIS connections time out entirely from this UK network (geo-blocking), `www.ercot.com` dashboards and `api.ercot.com` both return 403, and the new ERCOT public API requires a free registered subscription key — recorded in item 20 as a blocking input only Clyde can supply. MISO's `docs.misoenergy.org` DA ex-post LMP market report answered 200 keyless on the first probe (1.2 MB of real data), so MISO went first — the same measured keyless-first logic that picked GB.
- Added `adapters/miso.py`: keyless daily DA ex-post LMP CSVs, the eight named trading hubs selectable, EIA-930 `MISO` balancing-area carbon reusing the CAISO parse helpers, hourly points expanded to half-hour decision intervals, 31-day live-query cap. The parser anchors on the real file's structure (preamble lines before a `Node,Type,Value,HE 1..24` header) and its own stated convention that hours-ending are Eastern Standard Time year-round — a fixed UTC-5 with no daylight-saving fold, deliberately unlike NYISO's `America/New_York` handling, with a test asserting a July day still converts at UTC-5.
- Wired MISO through `app/markets.py` (locations, full market context with truthful labels: hub-precision price, BA-scoped carbon), both `serve.py` default-location maps, and the market selects on all four market-aware pages. Four offline tests on a fixture pruned from the genuinely downloaded report (hub/LMP row selection against MCC-component and loadzone contamination, fixed-offset conversion, missing-header fail-empty, eight-hub exposure): suite now **191 passing**.
- Caught the documented unsubstituted-token failure mode (blank-charts lesson #2) before it shipped: the script that inserted `<option value="MISO"__MISO__>` into four pages assumed all four use token replacement, but dashboard/planner/simulator use inline f-string conditionals, so the literal `__MISO__` would have rendered on three pages. Found by grepping for the token after the edit, fixed by matching each page's own mechanism, and verified by rendering the Operations page for MISO: option selected, zero token leaks.
- **Live verification:** `load_market("MISO", "indiana", planner=True)` returned **96 of 96 half-hours complete** — Indiana Hub day-ahead LMP **$26.87 to $116.32/MWh (a 4.3× intraday spread — a real scheduling prize)** and carbon 430.8–555.3 gCO₂/kWh, labelled with the honest EIA-930 fuel-mix fallback because the direct consumption rate was not yet published for the replay window. Whitespace and compile checks clean. All of today's work (discovery design, RedfishFleetProvider, inventory pipe, MISO) remains local and uncommitted.

### 2026-08-11 — session 25 continued: AI co-author trailers purged from published history

- Clyde found the public GitHub repository listing an AI assistant as a contributor and directed its immediate removal. Root cause: two pushed commits (`e806749`, `3b65aed`) carried a machine-added `Co-Authored-By:` trailer naming an AI tool — added by tooling defaults, in direct violation of this file's own writing rule. All commit author/committer identities were verified to be Clyde only; the trailers were the sole credit.
- Fix: rewrote every branch's commit messages with `git filter-branch --msg-filter` deleting the trailer lines (working tree stashed around the rewrite and restored untouched), verified zero `co-authored-by` matches in the full local log, then force-pushed `main` (`e806749` → `58c5e15`). Post-push verification: the remote log contains zero trailer matches and the GitHub contributors API returns only `ClydeLartey97`. Note the hashes for sessions ≤23's commits have therefore changed; any older hash referenced in this file before this entry refers to the pre-rewrite history.
- Strengthened the writing rule at the top of this file: machine-added `Co-Authored-By` trailers explicitly count as crediting, every committing session must strip them, and `git log --format='%B' | grep -i co-authored` must return nothing before any push.
- A factual sentence in the docs naming AI companies as an industry observation (about model-size disclosure) was deliberately left alone — the rule bars crediting tools as authors, not domain facts about the AI industry the product schedules workloads for.
