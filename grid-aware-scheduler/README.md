# Grid-Aware GPU Scheduler

**Start here → read `HANDOFF.md` first.** It contains the full project context, decisions made so far, prior art, and the exact next steps. This README is intentionally short — HANDOFF.md is the real source of truth and must be kept up to date at the end of every working session, regardless of which AI tool is used.
